// Twists fiber into rope. The mill's opposite number.
//
// Priced and paced exactly like the mill on purpose: the two are
// siblings, one at each end of the tower's construction economy, and
// they should feel like the same kind of room doing the same kind of
// work on a different material.
#![enable(implicit_some)]
RoomDef(
    id: "room.ropery",
    name: "Ropery",
    short: "ROP",
    // **Opens once the burner is lit**, which is where the opening
    // ends. `SYSTEMS.md` §6.11: a first-turn menu with twenty cards on
    // it is the loudest thing in the game and says nothing, so the menu
    // grows as the tower does. This is a fact about the tower, not the
    // player — the journal (§5.7) is the other kind of unlock and can
    // only filter a menu.
    // The comb establishes the fiber stream; only then is reserving
    // some of it as rope a meaningful choice.
    unlocked_by: "room.fiber_comb",
    category: Production,
    width: 2,
    // Three poles, paired with the comb's four. Seven poles establishes
    // the line instead of eight; the real price remains its floor space
    // and the hands that move fiber, not a second room tax.
    build_cost: [CostEntryDef(item: "item.poles", amount: 3)],
    recipe: RecipeDef(
        craft_ticks: 120,
        // Four coils are one elevator plus one tanglenet. Once the
        // tower holds that reserve the walk stops automatically, then
        // starts again as construction or a net spends it. Rope is a
        // finite infrastructure material, not a reason to babysit an
        // otherwise-correct production room.
        output_stock_target: 4,
        // Two fiber a coil against the mill's one bamboo a pole: rope is
        // dearer than poles, which is what stops it being a formality
        // and makes "can I afford a shaft" a real question.
        inputs: [IoEntryDef(item: "item.fiber", amount: 2, buffer_max: 6)],
        outputs: [IoEntryDef(item: "item.rope", amount: 1, buffer_max: 6)],
    ),
)
