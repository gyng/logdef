#![enable(implicit_some)]
#![enable(unwrap_newtypes)]
#![enable(unwrap_variant_newtypes)]
EnemyDef(
    id: "enemy.grunt",
    archetype: Grunt,
    hp: 30.0,
    speed: 60.0,
    damage: 8.0,
    attack_rate: 1.0,
    bounty: 3,
    threat: 1,
)
