#![enable(implicit_some)]
#![enable(unwrap_newtypes)]
#![enable(unwrap_variant_newtypes)]
TrinketDef(
    id: "trinket.tower_heart",
    name: "Tower Heart",
    category: Defensive,
    description: "Interior raiders are expelled in 8 seconds instead of 15.",
    effect: FasterBreachExpel(seconds: 8.0),
)
