#![enable(implicit_some)]
#![enable(unwrap_newtypes)]
#![enable(unwrap_variant_newtypes)]
BuildingDef(
    id: "building.lumberyard",
    building_type: Lumberyard,
    tier: T1,
    output_resource: Wood,
    production_rate: 4.0,
    operating_cost: 4,
    output_buffer_max: 3,
    build_tick_cost: 2,
    build_resource: Stone,
    build_resource_cost: 1,
)
