#![enable(implicit_some)]
#![enable(unwrap_newtypes)]
#![enable(unwrap_variant_newtypes)]
BuildingDef(
    id: "building.fletcher",
    building_type: Fletcher,
    tier: T1,
    output_resource: Arrows,
    production_rate: 6.0,
    operating_cost: 5,
    output_buffer_max: 4,
    build_tick_cost: 2,
    build_resource: Wood,
    build_resource_cost: 2,
    inputs: [
        (resource: Wood, amount_per_craft: 1, buffer_max: 4),
    ],
)
