// **Carries the load and takes the time.**
//
// Two more per trip, a fifth slower. Fewer, larger, slower trips — which is the right shape when the shaft is the bottleneck and the wrong one when it is not.
//
// Family argument and the full table: `SYSTEMS.md` §6.25.
#![enable(implicit_some)]
TraitDef(
    id: "trait.packhorse",
    name: "Packhorse",
    blurb: "carries the load and takes the time",
    weight: 40,
    carry_bonus: 2,
    walk_pct: 120,
)
