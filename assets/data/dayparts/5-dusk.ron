#![enable(implicit_some)]
DaypartDef(
    id: "daypart.dusk",
    name: "Dusk",
    start_permille: 760,
    // Exposure drops below `night_light_threshold` at about 835
    // per-mille, inside dusk, so the lamps come on here.
)
