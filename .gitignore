# OS/editor cruft
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
*.swo

# Environment/local config
.env
.env.*
!.env.example

# Rust
target/
target-*/
# cargo-mutants scratch output
mutants.out/
mutants.out.old/

# Node/web
node_modules/
.pnpm-store/
dist/
build/
.vite/
coverage/
*.tsbuildinfo

# wasm-pack output
web/pkg/

# The itch.io release cut, written by `make release`. A build output —
# reproducible from a tag, and half a megabyte of binary that would
# otherwise land in the history every time somebody cuts one.
understory-web.zip

# Playwright/browser automation
.playwright/
playwright-report/
test-results/
# Stills written by e2e/capture.spec.ts — a way to look at the game,
# not an artifact worth versioning.
web/capture/

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
__pycache__/
# Full-resolution generated art sources and generation sidecars.
art-src/
