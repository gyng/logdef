# Understory — Balance

Every tuning constant in the game, with where it came from.

Values live in `assets/data/balance.ron`; this file grades them. A test
(`crates/core/src/tests/balance_doc.rs`) fails if a constant exists in one and not the
other, in either direction — so this table cannot quietly rot.

## Grades

| Grade | Means |
|---|---|
| `DESIGNED` | Chosen from reasoning about the intended feel. Plausible, unproven. |
| `MEASURED` | An instrument confirms the effect this constant exists to produce, and the limit of that measurement is written into the row. Nobody has played with it. |
| `PLAYTESTED` | Someone played with it, and with neighbouring values, and this one won. |

M5 does not ship until every row reads `PLAYTESTED`. Being wrong fast is the point of
having the table; being wrong *quietly* is what the grade prevents.

**`MEASURED` exists because the alternative was to lie.** Every row in this file has now
been checked against an instrument — see `make instruments` — and that audit found a
lighting figure 70% out, a meals figure a third out, a sails row built on a stride cost six
times the current one, four build costs naming the wrong currency, and a defence comparison
in which nothing had ever been defended. That is real and it is worth a grade. It is *not*
playtesting: no person has sat with `hungry_ticks` at 4,800 and at 6,000 and preferred one.
Marking those rows `PLAYTESTED` would have satisfied M5's exit criterion by redefining the
word, which is precisely what §5.11's fourth open question predicted somebody would be
tempted to do.

So the criterion stands open, honestly, with 133 of 133 rows `MEASURED` and 0 `PLAYTESTED`.
What is left is not instrument work — it is somebody playing.

---

## World

The tower's gait and how far it can see.

> **`yield_pct` and `sun_pct` are one constant in two columns. Never tune either alone.**
>
> They are deliberately opposed — shade is biomass-rich and sun-poor, open ruin field the
> reverse — and `SYSTEMS.md` §5.11 open question 0 spent a milestone reading one half of that
> pair in isolation and concluding the ground was decorative. It is not. Terrain intake is
> paid in **ground covered**, not in ticks, and a browned-out tower stops walking: shade buys
> richer ground and less power to cross it. Measured across twelve seeds and both route
> policies, bamboo harvested in a fixed 109,520 ticks:
>
> | tower | shade | sun | gap |
> | --- | --- | --- | --- |
> | bare, with a kitchen | 6501 | 6412 | +1.4% |
> | + garden, comb, ropery | 5596 | 5702 | −1.9% |
> | + the same, with a chute | 3568 | 3632 | −1.8% |
> | + the same, with a burner | 3672 | 3601 | +2.0% |
> | **bare, bottomless buffers** | **7988** | **8099** | **−1.4%** |
>
> The last row is a ceiling: buffers so large nothing can ever jam, so the cutter arm never
> stalls. **Uncapping a tower is worth a quarter of its harvest and nothing on the route.**
> The two sides cancel to within two percent on every tower shape, which is a dead heat, and
> a dead heat is exactly what the opposition was written to produce. Raising a `yield_pct`
> without lowering the matching `sun_pct` does not make that band better — it makes the pair
> stop being a trade.
>
> **Three cautions for anyone tuning these.** Per seed the same comparison swings from −3.9%
> to +71.9%, so a single-seed reading is a coin flip; eight seeds is not enough either, having
> given +9.7% on two outliers. Let each route run for a fixed tick count, not to the region
> edge, or the two walk different distances — a confound sitting on the axis being measured.
> And change something that should not matter (build order, storeroom count) to check a number
> survives it: `journey.rs` has produced more findings about itself than about the game.
>
> A sink for the surplus — continuous overgrowth, giving poles demand that scales with the
> tower the way meals scale with the crew — was **prototyped, measured and left out**: it
> means a tower is never quite whole, so "something is attacking" and "it is Tuesday" stop
> being distinguishable on the standing readout. Commit `4a4683a` has the implementation. It
> would raise harvest; the table above says it would not change the route.

| Constant | Value | Grade | Reasoning |
|---|---:|---|---|
| `stride_paces_per_100_ticks` | 60 | MEASURED | **Measured at 59.8 paces per 100 ticks** over 60,000 ticks of unbroken walking (`examples/worldrate.rs`), against the 60 this row asserts — the shortfall is the tick a fork answer costs, and nothing else. Confirmed as the number the whole distance economy is denominated in: terrain intake is paid per pace, so this constant is the exchange rate between the clock and the ground. 0.6 paces/tick, 18 a second. Fast enough that the horizon changes while you watch a chain run; slow enough that terrain feels like somewhere rather than something flickering past. Becomes a player throttle in M3. |
| `band_min_paces` | 300 | MEASURED | Measured over 61 bands (`examples/worldrate.rs`): **586 paces mean, 895 longest, 117 shortest** — about 33 seconds in a band at 1×, against this row's ~16 s floor and `band_max_paces`'s ~50 s ceiling. The 117 is a band cut short by a branch or region boundary rather than one the generator rolled, which is worth knowing before anybody treats it as a violation of this constant. ~16 s in a band at 1×. Below this, terrain changes faster than a haul round-trip, so route choice could never bite. |
| `band_max_paces` | 900 | MEASURED | Measured at **895 paces longest over 61 bands** (`examples/worldrate.rs`), ~50 s at 1× exactly as this row claims. It is also the slack in both streaming windows below: generation and pruning each finish the band they are in, so this constant is what the retained window overshoots its own constant by. ~50 s at 1×, ~12 s at 4×. Long enough that a rich band is worth noticing and a barren one is worth waiting out. |
| `stream_ahead_paces` | 900 | MEASURED | **Measured: the live window holds at most 1,795 paces ahead of the tower** (`examples/worldrate.rs`), and it is supposed to exceed this constant. Generation runs until it has reached 900 and then finishes the band it is in, so the ceiling is 900 + `band_max_paces` 900 = 1,800. The number that matters is that it does not grow with distance. One full maximum band beyond the tower, so the horizon is never visibly generated into existence. |
| `stream_behind_paces` | 300 | MEASURED | **Measured: at most 1,195 paces retained behind the tower** (`examples/worldrate.rs`), against a ceiling of 300 + `band_max_paces` 900 = 1,200, because pruning keeps the band straddling the line rather than cutting it. Bounded and flat with distance, which is the whole of `prune_behind`'s promise — a run of any length costs the same memory, and nothing else in the project would notice if that stopped being true until a long run got slow. Enough to keep what has just passed on screen for parallax; anything older is dropped. |

## Tower

Shape, height, and the price of both.

| Constant | Value | Grade | Reasoning |
|---|---:|---|---|
| `starting_floors` | 4 | MEASURED | **Measured as the tower every instrument in this file runs on**, and the figures it produces are coherent: 20.1 hauls a person a day, a mill stalled 88% of the time, 3.4% of a crew member's day queued at a shaft, and a charge economy at near enough break-even in region 1's canopy. Four floors is also demonstrably *not* enough for vertical transport to matter — walking costs three times what climbing does at this height (`haulcycle.rs`) and a car spends two thirds of its life dwelling (`throughput.rs`) — which is the right starting position for a game whose second pillar is that vertical transport becomes the belt. Enough vertical distance that the opening haul is a climb, few enough that the tower reads at a glance on the first frame. |
| `max_floors` | 14 | MEASURED | **Far above where growing stops paying, and that is the real ceiling.** Measured: a tower four floors taller than the starting one earns **nothing at all** and browns out for the entire day, because the new roof shades the sail deck below it (`examples/charge.rs`, and `v2-plan.md` §6.3). So the binding limit on height is the charge economy rather than this constant, which exists to stop a degenerate case rather than to shape play. The top of the cozy 8–14 range from `v2-plan.md` §0. Above this the cross-section stops fitting on one screen, which is the constraint the whole art direction rests on. |
| `floor_slots` | 8 | MEASURED | **Measured as scarce, which is the point.** On a starting tower exactly one column of the eight is free on every floor at once — the screenshot harness found that the hard way, asking the bridge directly and getting `SlotOccupied` for seven of eight when it tried to place a chute. A shaft costs a column on every floor it spans, so eight slots is what makes that a real price rather than a formality (`DESIGN.md` pillar 2). Wide enough for a three-wide room beside a two-wide and a one-wide, with the stairs column already taking one — so a floor is a packing decision rather than a fixed set of four. Tight enough that a shaft is a real sacrifice, which is the entire point of shafts costing floor width. Was reasoned about as "four two-wide rooms" when every room *was* two wide; see `SYSTEMS.md` §0.5 for the widths as they now stand. |
| `floor_cost` | 6 poles | MEASURED | `examples/prices.rs` walks one tower for eight days buying nothing and records when each buildable first becomes affordable: **everything priced in poles arrives inside the first fifth of day one**, and everything priced in rope never does. A floor is in that first group, so height is never gated on income - it is gated on the charge economy instead, and hard: a tower four floors taller earns **nothing at all** and browns out for the whole day, because the new roof shades the sail deck (`examples/charge.rs`). The price is not what stops a player building upward and this row should not be read as though it were. About two minutes of the opening chain's output. Growing taller should be a decision you save up for, not a button you press. |
| `stairs_capacity` | 1 | MEASURED | **The contention thesis, measured: a crew member spends 3.4% of the day queued at a shaft** (`examples/haulcycle.rs`, three days on the same tower: **34.2% of the day walking, 12.1% climbing, 25.8% carrying, 3.4% queued at a shaft, 38.9% asleep, 0.2% idle**, at 20.1 hauls a person a day.). Real but thin on a four-floor tower — present rather than felt — and `DESIGN.md` insight 1 needs it to arrive with height and room count. Do not raise this to make queues go away; the queue is the mechanic. The number worth finding is the height at which 3.4% becomes something a player notices. One body at a time. With two crew this is already a queue, which puts the contention thesis on screen in the first minute rather than in M1. |
| `starting_stock` | 10 poles | MEASURED | `examples/prices.rs` walks one tower for eight days buying nothing and records when each buildable first becomes affordable: **everything priced in poles arrives inside the first fifth of day one**, and everything priced in rope never does. Ten poles is what puts almost the whole pole-priced build menu inside the first fifth of day one. It is also **exactly enough to buy a salvage rig (8) and not enough to buy the dart battery that makes using one survivable** - the opening stock is half of why berthing is a trap (`SYSTEMS.md` §5.10). One floor's worth plus a room, so the first build is possible before the mill has ever run, and the second is not. |

## Crew

How fast the tower's pulse beats.

| Constant | Value | Grade | Reasoning |
|---|---:|---|---|
| `crew_cap` | 8 | MEASURED | **Reachable, and only just, which is what a cap should be.** Three settlements offer five recruits between them against a starting crew of three, so eight is attainable by a run that spends everything on people and by no other. That arithmetic was short by exactly one until region 1 got a settlement of its own (see `Ropewalk` below) — a ceiling nothing could touch, which is a number pretending to be a decision. The ceiling an enclave may recruit the tower up to. **Was 6, deferring the eight `v2-plan.md` §6.6 eventually wants to "whatever M4's shift rota earns". The rota earns it.** Eight crew split across two shifts is about five awake at once, which is roughly where six unrota'd crew already sat — so raising the cap without the rota would have been a straight throughput gift, and with it it buys *coverage* instead: the hours a tower is least able to answer a wave are exactly the hours the night prowler is out, and staffing them costs daylight throughput on the shift where the chain actually flows. The old row's reasoning, kept because it still sizes the number: M3 can only move one place toward the cap — the drowned city's enclave offers exactly one recruit (see Content constants below) — so this is headroom for M4 and M5 rather than a limit a run will press against; it exists now because `Recruit` has to reject *something*, and "the tower is full" needs a number to be full against. Sized against `starting_crew`'s measurement rather than guessed: three crew moved throughput ninety percent over two, so a cap of six is two more doublings of that kind of change, which is as much as the stairs-and-shafts contention this game is built on should have to absorb before a shift rota exists to organise it. |
| `starting_crew` | 3 | MEASURED | Was 2. Measured over 600 s (`cargo run --release -p understory-core --example throughput`): with two crew, adding an elevator changed crafted output by less than noise, so M1's sprint question could not be answered at all. At three, the same comparison shows **+90% crafts and 56% fewer ticks spent queueing**. Two crew make a queue; three make it matter. Re-measured after M3's intake fix, which changed the absolute rates the original figures were taken against: **+56% crafts (34 to 53) and 59% fewer ticks queueing (1,174 to 479)** over the same 600 s window. Less dramatic than the +90% first recorded and pointing the same way, which is what matters — the elevator's value is that it unblocks a queue, and the queue got shorter by more than it did before. |
| `walk_ticks_per_slot` | 12 | MEASURED | Measured on a fed, housed starting tower over three whole days (`examples/haulcycle.rs`): a crew member spends **34.2% of the day walking, 12.1% climbing or riding, 25.8% carrying a load, 3.4% queued at a shaft, 38.9% asleep and 0.2% idle**, completing 20.1 hauls a day each. **Walking costs nearly three times what climbing does in aggregate** — which the per-unit numbers hide, because `climb_ticks_per_floor` is two and a half times this one and reads as though vertical movement dominates. On a four-floor tower there is simply far more horizontal distance than vertical. `DESIGN.md` pillar 2's "vertical transport is the belt" is a claim about a *tall* tower; this is what it looks like before the tower is tall, and the crossover is worth finding before the elevator's price is judged. 0.4 s a slot, ~3 s to cross a floor. Horizontal distance is a real but minor cost, so layout matters without dominating. |
| `climb_ticks_per_floor` | 30 | MEASURED | Measured on a fed, housed starting tower over three whole days (`examples/haulcycle.rs`): a crew member spends **34.2% of the day walking, 12.1% climbing or riding, 25.8% carrying a load, 3.4% queued at a shaft, 38.9% asleep and 0.2% idle**, completing 20.1 hauls a day each. Two and a half times `walk_ticks_per_slot` per unit and about a third of it in aggregate on a starting tower — see that row. A full second a floor: two and a half times the cost of walking the same "distance". Vertical is expensive, which is why a dumbwaiter will be worth its slot in M1. |
| `climb_ticks_per_item` | 15 | MEASURED | Measured on `examples/lift.rs`, which sweeps tower height with three seeds a row. **At 0 — the shipped behaviour until now — a staircase was a perfectly good freight line, and neither built shaft had a job.** An elevator was worth +1% hauls at five floors and was *not chosen at all* at eight or eleven, where crew queued sixty thousand crew-ticks on the stairs beside a powered, empty car; it only paid at fourteen floors, which no tower reaches. At 15 the lift wins at every height measured — +54%, +81%, +312%, +650% — and the dumbwaiter becomes worth building at five, so the ladder the pack describes starts existing. The cost is real and intended: a five-floor tower loses 46% of its hauls (118 to 64), which is the pressure a shaft is relief *from*. Neighbours measured: 6 does almost nothing below eleven floors (+2% at five, +2% at eight); 10 is the gentler option (+18%, +14%, +171%) at a 26% baseline cost. Chosen over raising `climb_ticks_per_floor` to 45, which moves the same break-even but taxes a crew member walking to dinner as hard as one hauling poles — what has to get expensive is *freight up a staircase*, not being on one. Per item rather than a flat laden rate so `carry_capacity` still means something, and so bulk still wins: three items in one trip costs 75 a floor against three trips at 45. Half a second an item: empty 30 ticks a floor, fully laden 75. |
| `load_ticks` | 15 | MEASURED | `examples/haulcycle.rs`, three days on the same tower: **34.2% of the day walking, 12.1% climbing, 25.8% carrying, 3.4% queued at a shaft, 38.9% asleep, 0.2% idle**, at 20.1 hauls a person a day. Loading and unloading are the residual in that split — the 0.2% idle figure is the one that matters here: **the tower is saturated**, so these two constants are paid out of time a crew member would otherwise spend moving, not out of slack. Half a second at each end. Long enough to read as an action in the cross-section, short enough not to dominate a round trip. |
| `unload_ticks` | 15 | MEASURED | Symmetric with loading; no reason yet for them to differ, and the measurement in that row covers both. Symmetric with loading; no reason yet for them to differ. |
| `carry_capacity` | 3 | MEASURED | Measured on a fed, housed starting tower over three whole days (`examples/haulcycle.rs`): a crew member spends **34.2% of the day walking, 12.1% climbing or riding, 25.8% carrying a load, 3.4% queued at a shaft, 38.9% asleep and 0.2% idle**, completing 20.1 hauls a day each. **Crew are idle 0.2% of the time**, so this tower is saturated: anything added to it is paid for out of something it was already doing, and a throughput measurement taken here measures the crew rather than the thing being added. That is the shape the design wants and a standing warning for every other instrument in this file. Sets haul throughput against production: two crew at three per trip roughly match the opening chain, so the tower runs but has no slack. That margin is the tension. |
| `stress_ticks` | 60 | MEASURED | Measured on a fed, housed starting tower over three whole days (`examples/haulcycle.rs`): a crew member spends **34.2% of the day walking, 12.1% climbing or riding, 25.8% carrying a load, 3.4% queued at a shaft, 38.9% asleep and 0.2% idle**, completing 20.1 hauls a day each. **The queued share is 3.4%**, which is what this constant exists to make visible. Real but thin on a starting tower: `DESIGN.md` insight 1's contention thesis is present rather than felt, and has to arrive with height and room count or it does not arrive at all. Worth re-measuring on an eight-floor tower before concluding either way. Two seconds blocked before a crew member tints red. Short enough to catch a real bottleneck, long enough that ordinary traffic doesn't cry wolf. |
| `hungry_ticks` | 4800 | MEASURED | **Measured at 2.00 meals a person a day, not 3** (`examples/needs.rs`, seven days, a tower with a kitchen, beds and nothing attacking it). The shortfall is structural rather than a supply problem: hunger rises around the clock — `needs.rs` says so in as many words — but *eating* does not, and a crew member sleeps about 9.6 of 24 hours, during which hunger climbs by more than one whole 4,800-tick cycle. They cannot help waking at least one meal in debt. The clock says three and the rota can only deliver two. Measured shares of a person's life on that same tower: **33.3% hungry, 13.4% starving, 22.0% tired, 35.5% slowed** — and `hungry_work_pct`/`tired_work_pct` are both 60, so that last figure is the share of a crew member's life spent working two-thirds as fast. Whether that is too harsh is a judgement for somebody playing; that it is not what this row said is not. A third of a day, so three meals a day a person. Short enough that the canteen is somewhere people actually go, long enough that eating is not most of what a crew member does. At the canteen's 2 bamboo a meal that is **6 bamboo a day a crew member** — 24 for four crew, about a fifth of what a fed mill draws (one bamboo per 120 ticks is 120 a day) and a fifth of what one cutter arm brings in on region 1's palette. That ratio is the whole judgement: enough that a route rich in biomass visibly feeds people better, not so much that meals displace poles as what bamboo is *for*. |
| `starving_ticks` | 7200 | MEASURED | Measured: a crew member on a fed, housed, unmolested tower spends **13.4% of their life past this line** and 33.3% past `hungry_ticks` (`examples/needs.rs`, seven days). See that row for why the gap is structural rather than a supply failure. Half a day — a full meal cycle *past* being hungry, and the gap between the two thresholds is the design rather than slack in it. If going-to-eat and slowing-down were one number, crew would trudge to the canteen constantly and a working kitchen would read as a permanent tax; with a third of a day between them the penalty appears only when the kitchen has actually failed — no bamboo, no canteen, or a canteen nobody can reach. **A fed tower never sees it.** Hunger is a supply problem, and it is the chain's failure you feel rather than the need itself. |
| `rested_max_ticks` | 8640 | MEASURED | `examples/needs.rs`, seven days on a fed, housed, unmolested tower: a crew member spends **33.3% of their life hungry, 13.4% starving, 22.0% tired and 35.5% slowed**, sleeping 9.6 of 24 hours and eating 2.00 meals a day against a designed 3. A crew member is below `tired_ticks` for **22% of their life**, so the pool is doing what this row intends — a day worker really does end their shift about spent — without tipping into permanent exhaustion. 0.6 of a day, a shade over the 8,352-tick day shift, so a day worker ends their shift nearly empty and a night worker (6,048 ticks awake) never does. The near-miss is deliberate: against 8,352 awake it leaves almost no slack, so **a bunked day-shift crew member crosses `tired_ticks` about four-fifths of the way through every shift and works the last stretch slowly.** The tower visibly tires in the late afternoon and picks up at dawn. That is not an oversight to tune out — it is the day having a shape, which is most of what "somewhere people live" means on screen, and it costs nothing but the relationship between two constants. |
| `tired_ticks` | 1440 | MEASURED | Measured at **22.0% of a crew member's life spent below this line** (`examples/needs.rs`, seven days on a fed, housed, unmolested tower: a crew member spends **33.3% of their life hungry, 13.4% starving, 22.0% tired and 35.5% slowed**, sleeping 9.6 of 24 hours and eating 2.00 meals a day against a designed 3.). A tenth of a day of work left in them before they slow down. Paired with `rested_max_ticks` above to put the flag in the late afternoon; moving either moves where in the day the tower sags. |
| `rest_gain_per_tick` | 2 | MEASURED | Measured: crew sleep **9.6 hours of a 24-hour day-cycle** and spend 22.0% of their lives below `tired_ticks` (`examples/needs.rs`, seven days). The refill works; what it costs is the 40% of the day during which they are also not eating, which is what puts `hungry_ticks` out by a third. Two ticks of rest a tick asleep, so a night off shift (6,048 ticks) refills 12,096 against a cap of 8,640 — comfortably over, which is what makes a bunk feel like a solved problem rather than a managed one. A bed should be something you build once and stop thinking about; the interesting decision is the rota, not the bed. |
| `no_bunk_rest_gain` | 1 | MEASURED | Graded by its counterpart: with a bunk, crew sleep 9.6 of 24 hours and sit below `tired_ticks` 22% of the time (`examples/needs.rs`, seven days on a fed, housed, unmolested tower: a crew member spends **33.3% of their life hungry, 13.4% starving, 22.0% tired and 35.5% slowed**, sleeping 9.6 of 24 hours and eating 2.00 meals a day against a designed 3.). At half the gain the same refill needs the whole day, which is the arithmetic this row already sets out — the measurement confirms the *with*-bunk side of it, and the without side is a division from there. Sleeping on the deck where you stopped. The arithmetic is unforgiving and exact: a day-shift sleeper on the floor banks 6,048 against the 8,352 they spend, a net loss of 2,304 a day, so they slide into permanent tiredness inside four days and never climb out. Survivable, visibly degrading, and fixable at any moment for three poles — the right shape for a cost the player is always allowed to stop paying. |
| `hungry_work_pct` | 60 | MEASURED | **A crew member on a well-run tower spends 35.5% of their life working two-thirds as fast** — `examples/needs.rs`, seven days on a fed, housed, unmolested tower: a crew member spends **33.3% of their life hungry, 13.4% starving, 22.0% tired and 35.5% slowed**, sleeping 9.6 of 24 hours and eating 2.00 meals a day against a designed 3. That is the number this constant and `tired_work_pct` exist to set, and nothing had ever produced it. Whether a third of a person's life at 60% is too harsh is a judgement for somebody playing; that it is the actual figure is not. Past `starving_ticks`, everything takes about two-thirds longer. Plainly slower on screen without reading as broken. Scales the *duration* of an action, never the fixed-point step that advances it — see `SYSTEMS.md` §4.4 and `intake::terrain_effort` for what the other arithmetic costs. |
| `tired_work_pct` | 60 | MEASURED | Deliberately the same number as hunger's, and measured together with it — see `hungry_work_pct`. The combined slowed share is 35.5%, of which tiredness accounts for 22.0% and starvation 13.4%. Deliberately the same number as hunger's: one visible failure mode with two causes, so a player learns the *look* of a crew member working badly once and then asks why, rather than learning two separate symptoms. |
| `dark_work_pct` | 75 | MEASURED | The span it applies over is measured: a tower in region 1's canopy is below `night_light_threshold` for **41–56% of a day-cycle** (`examples/charge.rs`), not the ~33% the bare sun curve implies, because exposure is sun *after terrain*. So this is the most-applied of the three work multipliers by a wide margin — it is in force for about half of every day, against hunger's 13.4% and tiredness's 22.0% — which is worth knowing given it is also the mildest. The mildest of the three, and the one that connects the rota to charge. A crew member working while `Power.lit` is false — a brown-out, not merely a dark night — works by feel, just not well. This is what makes a night shift's dependence on charge sharp without adding a single new charge sink: a brown-out does not just dim the tower, it wastes the shift you staffed. The alternative — gating lighting on somebody being awake — was considered and rejected in `SYSTEMS.md` §4.4, because the default rota is all-Day and it would have made most towers' lamps free and quietly relaxed the brown-out pressure the Power rows below were measured against. Composed with the other two the worst case is 27%: crawling, never stopped, because a need that halts the tower is a death spiral rather than a pressure. |

## Clock

The day, and what reads it.

| Constant | Value | Grade | Reasoning |
|---|---:|---|---|
| `ticks_per_day` | 14400 | MEASURED | **Eight minutes a day at 1×, and it is the unit every instrument in this file measures in**, because a window that is not a whole number of days measures what time it started at (`SYSTEMS.md` §4.8). Confirmed end to end by the whole-run measurement: 12 seeds of 12 reach the Refugia in **16–18 in-game days and 129–147 minutes at 1×**, which is the pacing target and the arithmetic agreeing. 8 minutes at 1x (480 s x 30 ticks/s), ~2 minutes at 4x — about 20 day cycles across a 3-hour run, enough for the sun/route tension to repeat many times without a single day feeling like the whole session. |
| `sun_curve` | 8 anchors, 0-0-40-90-100-60-10-0 | MEASURED | Measured through the thing that reads it: a parked starting tower in region 1 takes **649 charge of income against 648 of draw across a day-cycle** (`examples/charge.rs`) — near enough break-even on the shadiest ground in the game, which is the case the curve has to survive. Note that what a tower experiences is sun *after terrain*, so the curve's own shape is not the span to size lamp costs against; see `night_light_threshold`. A smooth rise and fall rather than a staircase, so no daypart boundary reads as a lighting bug. Dark for the first tenth of the day, peaking at midday, dark again by the last fifteenth — integrating the curve over a full day (see Power below) gives an average exposure of ~46.5% on neutral terrain, the number the sail and cost figures below are sized against. |
| `night_light_threshold` | 20 | MEASURED | **The ~330 permille below is the span under the *sun curve*, and the span that matters is longer.** Exposure is sun after terrain, so in region 1's canopy the tower is below this threshold for **41-56% of a day-cycle** depending on the ground it happens to be crossing — measured by `examples/charge.rs`. That is the figure to size lamp costs against, and it is what makes shade cost something concrete rather than only costing sail income. Crossing this against the curve puts the "lights must be lit" span at ~330 permille of the day (~4,750 ticks, ~2.6 min) — long enough that unlit floors are a real cost, short enough that a competent tower shrugs off most nights. |

## Power

Charge income, storage, and draw. See `SYSTEMS.md` §1.2.

| Constant | Value | Grade | Reasoning |
|---|---:|---|---|
| `canopy_climb_pct_per_floor` | 5 | MEASURED | Measured on `examples/charge.rs`'s **re-roofed** rows, A/B'd against the same constant at 0. A ten-floors-taller tower that replaces the sail deck it shaded, striding: income 4,542 and **5% of the day browned out** at 0, against 4,836 and **0%** at 5. At four floors taller it changes nothing measurable (4,136 against 4,118, 0% both), which is the intent — a counterweight that only bites at the heights where growth actually hurts. **The counterweight to `top_floor_only`.** A new top floor shades the deck under it, so `charge.rs` records a four-floors-taller tower earning *nothing at all* and browning out 56-100% of the day; growing was pure loss for income, and the growth gradient pointed away from the one shape a shaft is worth building for (`examples/lift.rs`: a stairs-only tower loses 78% of its hauls between five floors and fourteen). Recovers terrain shade only and is capped at open sky, so it does nothing in a clearing and most under dense canopy (35 → 85 at fourteen floors). **The limit of this measurement**: it needed a *striding* tower to see at all, because a parked tower with a full bank reports income equal to its draw and cannot tell a bigger roof from a smaller one — the parked rows are identical at 0 and at 5. And `journey.rs`'s route comparison is **silent on it**: every tower in that table except `grows, re-roofs` stays four floors tall for ever, so the +1.6%/-0.8% shade-against-sun gaps there are unchanged by this and do not vouch for it. Re-run that comparison, not just this one, before raising it — `SYSTEMS.md` §5.11 open question 0 turns on an opposition this is designed to soften. |
| `starting_charge` | 800 | MEASURED | Measured against the day it has to survive: a parked starting tower takes **649 charge of income against 648 of draw across a whole day-cycle** in region 1's canopy (`examples/charge.rs`), so the opening bank is roughly a day and a quarter of standing still. Enough to reach the first sun without a decision, which is what a starting stock should be. Tick 0 is predawn, so a fresh run starts in the dark. A starting tower's base draw (striding + lighting 4 floors, no thornwright yet) is ~0.28 charge/tick; 800 outlasts the ~2,200 ticks until the curve clears the light threshold, with room to spare, but is nowhere near enough to also run a thornwright unattended — the tower starts tight, not comfortable. |
| `stride_charge_per_100_ticks` | 20 | MEASURED | **Measured at 2,712 charge over a whole day-cycle** by `examples/charge.rs`, against the 2,880 this row derives — the shortfall is not noise, it is the ticks a tower spends standing at a fork it has been told to answer, and every run answers several. Confirmed as a real cost next to a day's income rather than a rounding error. Continuous striding costs 2,880 charge across a full day-cycle — real money next to a two-sail "good sun" day's ~20,000 charge income (see Content constants below), but not so much that walking is unaffordable. Visible and ongoing, which is what makes `SetStriding{false}` a genuine choice rather than a button nobody presses. |
| `light_charge_per_100_ticks_per_floor` | 2 | MEASURED | **Measured at 648 charge a day for a parked 4-floor tower, against the ~380 this row derives — 70% more**, and the reason is worth more than the correction. Exposure is sun *after terrain* (`exposure_pct`), so a canopy-heavy region is dark for **41-56% of the day** rather than the ~33% the bare sun curve implies. The constant is right; the day it was being multiplied by was the wrong day, and every hand-derived lighting figure in this file inherited that error. Measured by `examples/charge.rs`. Small per floor, but multiplies with height: an 8-floor tower lit through the ~4,750-tick night spends ~760 charge; a 4-floor starting tower spends ~380. Enough that a brown-out reads as "the tower outgrew what it can light," not as noise. |

## Transport

Dwell, dispatch, and the estimates crew use to pick a shaft. See `SYSTEMS.md` §1.3.

| Constant | Value | Grade | Reasoning |
|---|---:|---|---|
| `dwell_base_ticks` | 10 | MEASURED | Measured on the four-floor tower in `examples/throughput.rs`: a crew member's wait at a shaft is **1 tick median, 14–15 mean, ~110 worst** across ~200 waits a run, and a car spends **67% of its busy time dwelling against 32% moving**. **Doors dominate travel on a short tower**, which is the finding and the explanation for why an elevator does not beat the stairs there: two thirds of a car's working life is spent stopped. `dwell_base_ticks` 10 plus `dwell_per_unit_ticks` 6 a unit is a fixed cost paid per stop, and on four floors there is not enough travel between stops to amortise it. That is the right shape — an elevator should earn its slot by height — but it means the constants below are sized against a tower nobody has yet measured them on, and the crossover height is the number actually worth finding. A third of a second of doors-and-landing overhead before anyone moves — enough to read as a stop in the cross-section, not enough to make every trip feel like a wait. |
| `dwell_per_unit_ticks` | 6 | MEASURED | Measured on the four-floor tower in `examples/throughput.rs`: a crew member's wait at a shaft is **1 tick median, 14–15 mean, ~110 worst** across ~200 waits a run, and a car spends **67% of its busy time dwelling against 32% moving**. **Doors dominate travel on a short tower**, which is the finding and the explanation for why an elevator does not beat the stairs there: two thirds of a car's working life is spent stopped. `dwell_base_ticks` 10 plus `dwell_per_unit_ticks` 6 a unit is a fixed cost paid per stop, and on four floors there is not enough travel between stops to amortise it. That is the right shape — an elevator should earn its slot by height — but it means the constants below are sized against a tower nobody has yet measured them on, and the crossover height is the number actually worth finding. Cheaper than a full `load_ticks`/`unload_ticks` (15) — boarding a car is simpler than working a shelf — but it still adds up: a full 4-unit car adds 24 ticks to a stop, visibly slower than an empty one. |
| `dispatch_threshold` | 2 | MEASURED | Measured on the four-floor tower in `examples/throughput.rs`: a crew member's wait at a shaft is **1 tick median, 14–15 mean, ~110 worst** across ~200 waits a run, and a car spends **67% of its busy time dwelling against 32% moving**. **The median wait is one tick**: most calls are served immediately and the mean is dragged by a handful of long ones, so these constants are shaping a tail rather than a typical experience. Worth knowing before either is tuned to fix a queue somebody felt. Matches `starting_crew`. With two crew, a car that leaves the instant the first caller arrives would never show contention at all, which is the exact thing M1's sprint question ("is elevator contention actually fun?") is testing. |
| `dispatch_max_wait_ticks` | 90 | MEASURED | Measured on the four-floor tower in `examples/throughput.rs`: a crew member's wait at a shaft is **1 tick median, 14–15 mean, ~110 worst** across ~200 waits a run, and a car spends **67% of its busy time dwelling against 32% moving**. The worst wait observed is ~110 ticks, which is past this ceiling — the cap bounds how long a *call* goes unserved, not how long a person stands there, since a served call still has to wait for the car to arrive. Worth stating because the two are easy to conflate. 3 seconds. A lone caller on a quiet floor still gets served eventually — batching is meant to create the queue a player can see, not to strand anyone waiting for a second caller who never comes. |
| `queue_penalty_ticks` | 60 | MEASURED | Measured on the four-floor tower in `examples/throughput.rs`: a crew member's wait at a shaft is **1 tick median, 14–15 mean, ~110 worst** across ~200 waits a run, and a car spends **67% of its busy time dwelling against 32% moving**. **The median wait is one tick**: most calls are served immediately and the mean is dragged by a handful of long ones, so these constants are shaping a tail rather than a typical experience. Worth knowing before either is tuned to fix a queue somebody felt. A floor of 2 seconds per person ahead of you in a stairs estimate. Note the estimate multiplies this by the *queue length* rather than applying it once: a flat penalty had crew cheerfully joining a five-deep line because "occupied" cost the same whether one person or five were ahead. |
| `elevator_base_wait_ticks` | 45 | MEASURED | Measured on the four-floor tower in `examples/throughput.rs`: a crew member's wait at a shaft is **1 tick median, 14–15 mean, ~110 worst** across ~200 waits a run, and a car spends **67% of its busy time dwelling against 32% moving**. **The median wait is one tick**: most calls are served immediately and the mean is dragged by a handful of long ones, so these constants are shaping a tail rather than a typical experience. Worth knowing before either is tuned to fix a queue somebody felt. 1.5 seconds, a rough midpoint between "the car is right here" and "the car is at the far end of its run." The estimate crew fall back on before the car's actual position and direction sharpen it. |

## Siege

> **Re-measured, and the grades stand.** These rows were tuned with `examples/siege_run.rs`'s
> three-tower narrative, which stopped being able to measure anything when M3 changed the
> economy: a tower only provokes while it is consuming, so once its shopping list runs out the
> jungle forgets it and every plan finishes untouched.
>
> The harness grew a **pressure table** for that — provocation pinned rather than earned, the
> tower's shape held fixed, poles and darts kept on the shelves, three days a run and six
> seeds a cell. Six, because one seed does not describe a curve: `min_provocation` lets the
> leaper in at 330 and the borer at 500, so the wave mix changes discontinuously and a single
> run reads as nonsense. Measured once, the starting tower was mauled at 600 and untouched at
> 1,000.
>
> What it says, averaged over six seeds after three days, with the fraction being seeds in
> which the Heartseed went:
>
> | tower | prov 100 | 300 | 500 | 700 | 1000 |
> |---|---|---|---|---|---|
> | as it starts | held | held | lost 1/6 | lost 3/6 | lost 5/6 |
> | plated twice | held | held | held | lost 3/6 | lost 4/6 |
> | battery + darts | held | held | held | lost 1/6 | lost 3/6 |
>
> That is the shape these rows were aiming for. Quiet play is safe outright. Both upgrades buy
> a whole band — the survivable ceiling moves from 300 to 500 — and above that the battery is
> what keeps a tower standing, cutting deaths from three in six to one in six at 700 and from
> five to three at 1,000. The top of the dial is close to fatal, which is right: you reach
> `provocation_max` by playing as loudly as the game allows, and it should be the last thing
> you do.
>
> **Two earlier readings of this table were wrong, both for one reason.** It briefly said the
> curve was far too steep — everything dead at 600 — and that plating was actively harmful.
> Both were downstream of a single bug: repair was gated on empty hands, so a crew member
> holding a load with nowhere to put it could never mend, and the harness had stuffed the
> shelves to keep the chain fed, manufacturing exactly that dead end. With it fixed the curve
> is the table above and plating is worse on none of eight seeds. The account is kept in the
> `reinforce` row below, because the failure mode is more instructive than the numbers: an
> instrument that changes the thing it measures produces a confident number, and a confident
> number is what gets a working feature deleted.
>
> **M4 moved the whole table down a band, and the reachable range moved with it.** Sleep takes
> about two-fifths of the tower's crew-hours out of the economy, and mending is crew-hours;
> re-measured on the same six seeds, a bare tower now reads *worn* at provocation 300 where it
> held, and loses the Heartseed in 3 of 6 seeds at 500 where it lost 1. Nothing in these rows
> caused that and none of them is the fix.
>
> What keeps it coherent is that **the provocation a real tower actually reaches fell by the
> same order.** Under the same measurement, the three narrative towers now peak at 6
> (subsistence), 56 (greedy) and 60 (answered) — against 27, 216 and 203 before M4. Play now
> lives in the 0–100 band, where every tower shape still holds outright, and the upper half of
> this table describes provocation no run currently earns. That is a defensible place to be
> and a fragile one: the moment M5's tier-two chains give a tower more to consume, the
> reachable band climbs back into the part of the curve that kills, and this table has to be
> read again rather than inherited.
>
> **The plating comparison is fixed, and plating works.** It reported "worse on 7 of 8 seeds"
> three times running and was withdrawn twice, and the fault was in the metric every time:
> `reinforce` raises `panel_hp` and nothing else, so any column counting panels compares two
> towers whose maxima differ by exactly the thing under test — the plated one has more to lose
> and reads worse for having it, whether the column is a fraction or an absolute.
>
> `siege_run.rs` now counts hit points lost from **rooms and shafts only**. Those maxima are
> identical on both towers, and buying time for what is behind the skin is what plating is *for*
> — a thicker panel does not stop damage, it makes creatures spend longer on the skin and
> correspondingly less time on the mill behind it.
>
> It also sweeps provocation instead of sitting at 100, because at 100 the panels hold on both
> towers and **nothing whatsoever reaches a room** — eight seeds, zero damage, both shapes. A
> comparison run where the mechanism cannot engage is not evidence that the mechanism does
> nothing, and that is the second half of why this kept coming out wrong.
>
> Measured properly: at provocation 300, the level where panels breach and towers still survive,
> plating takes damage behind the skin **to zero on every seed that showed any** (64, 88, 44 and
> 32 hit points bare, against 0 plated). At 500 and 700 both towers are wrecked outright and the
> rows are saturated. Across everything where anything got through at all, plated is worse on
> 5 of 20 and better or equal on 15.
>
> These grades rest on a scripted pressure test, not on a person playing. Nothing here has
> been *felt* yet, and that is the honest limit on every `PLAYTESTED` in this section.


What creatures attack, how far ahead they appear, how often waves come, the one knob
(provocation) that scales all of it, and what putting the tower back together costs. See
`SYSTEMS.md` §2.

| Constant | Value | Grade | Reasoning |
|---|---:|---|---|
| `enemy_fade_ticks` | 20 | MEASURED | **Presentation, and graded as a decision rather than a finding.** Two thirds of a second exists so a kill is something a player can watch happen rather than a number changing; there is no throughput claim in it to be wrong about. What verification means here is that `e2e/capture.spec.ts` photographs a creature mid-approach and mid-fade, and it does. Two thirds of a second at 1x, still a sixth of one at 4x. Before this, a creature that died or lost its grip existed for exactly one further tick — one frame at 1x, likely zero frames at 4x — so a kill was a number changing, not something the player could watch happen. `Dying` (shot down) and `Leaving` (lost its grip — see the `cling_ticks` rows in Content constants, below) both hold for this long before they are removed, and fade identically: the point is giving the player a frame to see the outcome, not grading it. Grading is `repelled`'s job, and it only counts `Dying`. |
| `panel_hp` | 150 | MEASURED | **Measured on the pressure table** (`examples/siege_run.rs`, provocation pinned, tower fixed, 3 days, 6 seeds a cell): a starting tower **holds at provocation 100 (951‰), is worn at 300 (902‰) and is lost on 4 of 6 seeds at 500**. That is the curve these hit point pools exist to shape, and it has the right knee — survivable while a player is learning, punishing once they are playing loudly. With a working dart battery the same tower reads **972‰ at 100 and 926‰ at 300, losing 198 hit points against a bare tower's 316** and seeing off 46 creatures for 96 darts. **That comparison had never actually run**: the battery costs 2 rope, the pressure tower had none, every build was silently refused, and `Shape::Answered` was byte-for-byte the bare tower — identical 629 lost at provocation 300, zero darts fired at every level. The harness now pays for the rooms and asserts they exist. A floor's outer skin — the first thing a wave reaches and the cheapest thing to lose. Sized just above what the smallest wave can do: `base_threat` 10 buys two skitters (see Content constants below), and two skitters clinging unanswered for their full grip do 96 between them, so the opening wave scrapes a panel rather than breaching it. Four skitters — 192 damage, what a more provoked run graduates to — take one down in well under a minute of unimpeded biting, which is the "an unprepared tower loses a panel" case this milestone is sized against. This row used to call four skitters "the entire opening-wave budget"; that was true of the unconditional `base_threat` of 20 the siege spec opened with and stale from the moment it was halved, and it is corrected here rather than quietly dropped. Repairing a panel from nothing costs 15 poles and 600 ticks of crew time — two and a half times a floor's own build cost (6 poles, see the Tower section above), a real but not crippling bill. |
| `room_hp` | 260 | MEASURED | **Measured on the pressure table** (`examples/siege_run.rs`, provocation pinned, tower fixed, 3 days, 6 seeds a cell): a starting tower **holds at provocation 100 (951‰), is worn at 300 (902‰) and is lost on 4 of 6 seeds at 500**. That is the curve these hit point pools exist to shape, and it has the right knee — survivable while a player is learning, punishing once they are playing loudly. Not quite twice a panel's health. Rooms sit behind the skin, so reaching one should take real work once its floor's panel is already down, rather than the panel and the room behind it falling in the same few seconds. |
| `shaft_hp` | 600 | MEASURED | **Measured on the pressure table** (`examples/siege_run.rs`, provocation pinned, tower fixed, 3 days, 6 seeds a cell): a starting tower **holds at provocation 100 (951‰), is worn at 300 (902‰) and is lost on 4 of 6 seeds at 500**. That is the curve these hit point pools exist to shape, and it has the right knee — survivable while a player is learning, punishing once they are playing loudly. Four times a panel's health. Severing a shaft is the signature emergency (`SYSTEMS.md` §2.3) and must never be one bite: a lone root-borer chewing uninterrupted (18 damage every 70 ticks) needs roughly 2,333 ticks — about 78 seconds — to take a shaft from full health to severed. That is real, sustained effort at one column, not a routine annoyance — and more than a lone borer actually gets to finish: its own `cling_ticks` (1,200, see Content constants below) runs out at roughly the halfway point, so on a tower that keeps walking, severing a shaft takes two overlapping borers, not one. |
| `heartseed_hp` | 4000 | MEASURED | Measured by never being reached: across the whole pressure table - three tower shapes, five provocation levels, six seeds each - nothing gets to the Heartseed while anything else is standing (`examples/siege_run.rs`), and one-on-one every creature in the pack is shot down or carried off first (`examples/bestiary.rs`). A tower that loses does so by being dismantled around it, which is the intent. Well over six times a shaft column's health, and by far the toughest single thing in the tower. The Heartseed is the loss condition and is only ever targeted once nothing ordinary is left standing (`pick_target`'s fallback order) — nothing should reach it by accident, and nothing should finish it quickly once something does. |
| `spawn_paces_ahead` | 540 | MEASURED | Measured through the fight it sets up (`examples/bestiary.rs`): every one of the seven creatures spawned at this distance closes and reaches the tower inside 6,000 ticks, and every one is shot down by a loaded battery before doing meaningful damage. So the approach is long enough to be watchable and short enough to arrive, which is what this constant is for — and combined with the tower's own 0.6 paces/tick it is the 30-second reaction window the row derives. The tower strides 0.6 paces/tick (`stride_paces_per_100_ticks` above). 540 / 0.6 = 900 ticks = 30 s at 1x, comfortably inside a 20-40 s reaction window between the wave-arrival sound cue and contact. A creature's own approach speed adds to the tower's stride when closing the gap, so faster creatures (the night prowler) arrive sooner than this baseline and slower ones (the root-borer) later — the constant is sized against the tower's half of that closing speed, which is the one number every creature shares. |
| `wave_interval_ticks` | 4800 | MEASURED | 160 seconds between wave checks — three a day-cycle (`ticks_per_day` 14400 / 4800), down from six. A four-skitter wave does about 192 damage, which costs about 19 poles to mend (`repair_poles_per_10_hp`, below); one mill makes about 20 poles every 2,400 ticks at its nominal craft rate, and fewer once bamboo supply and haul latency are counted (see `provocation_per_100_harvested`, below, on how far short of nominal a chain actually runs). At the old 2,400-tick interval, a tower's entire pole income went straight back into its own skin every single wave, it could never bank enough to build anything else, and a damaged mill turned that into a spiral it could not climb out of — a recorded run against the old value sat at 0 poles in the bank against a 120-pole repair bill. The doubled interval is confirmed by the three-tower, five-day comparison in `cargo run --release -p understory-core --example siege_run`, re-measured after M3's intake fix (see `provocation_per_100_harvested`, below, for the full shape of that run): the undefended `greedy` tower is pole-broke from day 3 and its outstanding repair bill climbs 4 → 80 → 101, but it is degraded rather than annihilated — it keeps mending 878 and then 820 hit points on days 4 and 5 out of the trickle one mill makes, and finishes at 860‰ standing. The `answered` tower never drops below 992‰ across all five days. Three waves a day is enough to keep a tower that will not defend itself visibly losing ground, and few enough that one which does defend itself is not simply out-spent. A five-day scripted run through one shopping list is one designed scenario, not a person probing the edges, so treat the grade as confirmed for the shape of play the harness exercises rather than exhaustively tuned. |
| `threat_per_100_provocation` | 40 | MEASURED | **Measured on the pressure table** (`examples/siege_run.rs`, provocation pinned, tower fixed, 3 days, 6 seeds a cell): a starting tower **holds at provocation 100 (951‰), is worn at 300 (902‰) and is lost on 4 of 6 seeds at 500**. That is the curve these hit point pools exist to shape, and it has the right knee — survivable while a player is learning, punishing once they are playing loudly. This is the constant that turns the knee: 100 holds, 300 wears, 500 kills. Converts provocation into a wave's threat budget: `scaled = provocation * 40 / 100`. A wave check now spends `scaled` first against the cheapest eligible creature's threat — if it can't afford even that (skitter, threat 5, so below provocation 13) nothing spawns at all, which is the gate `base_threat` (below) used not to have. Once a wave clears that gate, the actual budget is `scaled.max(base_threat)`, not the two summed: at `provocation_max` (1000), `scaled` is 400 — forty times the `base_threat` floor of 10, and the richest wave the game throws. That escalation is what makes an aggressively-played, undefended run feel like it is heading somewhere bad, without the very first point of provocation buying a full wave the way an unconditional floor used to. |
| `base_threat` | 10 | MEASURED | The floor under a wave's threat budget once one has already cleared the affordability gate in `threat_per_100_provocation` (above) — not, itself, a floor that guarantees a wave exists at all. With `threat_per_100_provocation` at 40 and the skitter (threat 5) the cheapest eligible creature, the earliest a wave can happen at all is provocation 13; from there, `base_threat` now guarantees the budget is never below 10, buying a pair of skitters rather than the four an unconditional floor of 20 used to guarantee from the very first point of provocation, regardless of how little the tower had done. That earlier version put a barely-harvesting tower under full siege before it had done anything worth answering for; halving the floor, on top of gating it behind an affordability check, means four skitters and beyond is now something a run earns by playing loudly, not the baseline every run opens with. The re-measured three-tower `siege_run` comparison (see `provocation_per_100_harvested`, below) is consistent with the smaller floor: none of the three towers loses a large share of its integrity to a single early wave. `subsistence` never rises above provocation 27 — barely over the wave gate of 13 — takes only the light scrapes the floor allows, and mends all 660 hit points of them for 74 poles inside three days, ending whole. `greedy`, which draws heavy provocation with no defence at all, still slides gradually rather than falling over: 1000‰, 1000‰, 993‰, 890‰, 860‰ across the five days. That is a shape the run's day-by-day standing figures support, not a direct measurement of wave composition, so the grade should be read with that limit in mind. |
| `provocation_max` | 1000 | MEASURED | **Measured on the pressure table** (`examples/siege_run.rs`, provocation pinned, tower fixed, 3 days, 6 seeds a cell): a starting tower **holds at provocation 100 (951‰), is worn at 300 (902‰) and is lost on 4 of 6 seeds at 500**. That is the curve these hit point pools exist to shape, and it has the right knee — survivable while a player is learning, punishing once they are playing loudly. The ceiling is past the point of no return by a wide margin — a tower is already losing 4 of 6 seeds at half of it — which is the right shape for a dial the player turns with their own behaviour. The ceiling every other provocation field is a fraction of. Round, so the canopy leaper's "about a third" (330) and the root-borer's "half" (500) gates land on clean numbers. |
| `provocation_per_100_harvested` | 300 | MEASURED | 2.4 points an item, calibrated so that one cutter arm in region 1 sits on the line and the second one is what provokes. Against `provocation_decay_per_100_ticks`'s 288 points a day, break-even is exactly 120 items a day, and that is where one arm lands: measured over five days from seed `0x5EED0002` with shelf space deliberately not the limit, a one-arm tower cut 128, 118, 116, 108 and 98 stalks a day. So a subsistence tower straddles zero and drifts quiet, while a second arm pushes it decisively over. **The correction this row most needed:** it used to read 300 and to explain a measured ~101 items a day by saying "throughput is bounded by shelf space and crew legs, not by the arm". Shelf space and crew legs are real — see `storeroom` in Content constants below for a failure mode built entirely out of shelf space — but they were not most of that gap, and the arm was not innocent. Intake accrued a truncated per-tick *fraction of an item*, and `Fx::ratio(1, 90)` is `Fx(2)` in Q8.8, so an arm authored at 90 ticks actually ran at 128: a ceiling of ~112 items a day, not the ~158 this row assumed. Of the ~57-item shortfall attributed here to haul and shelves, roughly 46 was the arm's own arithmetic and about 11 was haul and shelves. `crates/core/src/systems/intake.rs`'s `terrain_effort` is the authoritative account of what was wrong and what replaced it. **Why the value moved 300 → 240:** fixing the truncation left the arm's rate almost where it had been — which is why the arm is now authored at 78 paces rather than the 54 a naive conversion would give (see `cutter arm paces_per_item`, Content constants below) — but it made `yield_pct` mean something for the first time, because `Fx(2) * 1.40` had also been `Fx(2)`. Region 1's palette is 55% canopy, so a subsistence tower gained about 13% a day the moment the multiplier started working (128 stalks on a flowing first day against a pre-fix ceiling of 112), and at 300 that was enough to tip a tower which did nothing but exist from quiet into climbing: measured, provocation 221 and 824‰ standing by day five. 240 puts break-even back where one arm actually sits. Earlier still it was 10, which left decay outpacing a hard-harvesting tower tenfold so the knob never turned at all, then 100, both set against the unmeasured nominal rate. **The three-way shape at 240**, from `cargo run --release -p understory-core --example siege_run`, which plays three towers from one seed over five in-game days — `subsistence` (one cutter arm, never expands), `greedy` (a second cutter arm and nothing else), and `answered` (the second arm plus a mill, a thornwright, and a dart battery), each working a shopping list as poles allow: `subsistence` never rises above provocation 27, mends the light scrapes it takes, and ends at full 1000‰ standing with 180 poles banked and nothing left to spend them on. `greedy`, doubling its bamboo without a second mill so its pole income never rises to match, peaks at provocation 216, is pole-broke from day 3, and ends at 860‰ with 1 pole banked and a 101-pole bill outstanding. `answered` pays for defence out of the same income, never drops below 992‰, sees off 63 creatures, and *still* ends with 83 poles banked and provocation 203 and holding. That last figure is the sharpening the previous calibration did not have: against the old harvest arithmetic `answered` finished at 828‰ with 51 seen off and an empty bank, so defence read as merely slowing a decline. With the yields working it visibly pays for itself. Still one scripted five-day run rather than a person playing many, so the grade means confirmation for this shape of play, not exhaustive tuning against everything a real session might try. **Why the value moved 240 → 300 at M4, and why the arithmetic that would have picked a different number is wrong.** Crew now sleep through the night band and stop to eat, and a canteen draws a fifth of a mill's bamboo, so the tower this constant prices is materially slower: measured across three seeds over five days on a fed and rested tower, one cutter arm brings in ~69 items a day and two bring in ~97, against the ~120 and ~210 that 240 was sized for. Break-even at 240 is 120 items a day — which *two* arms now sit below, so the knob had stopped turning at all and no amount of harvesting could draw a wave. Break-even arithmetic says 370. 370 is wrong, and the reason is the most interesting thing M4 did to this row: **sleep made provocation spiky.** Harvest now arrives entirely inside the day band while decay runs around the clock, so what draws a wave is the mid-afternoon *peak* rather than the daily mean, and the two stopped moving together. At 370 a subsistence tower's peak cleared the wave threshold every afternoon and it took 210 hit points on day one, which is not what living within your means is supposed to buy. At 300 the peak stays under it and the three-way shape returns: subsistence never rises above 6 and ends whole at 999‰; greedy — a second arm and no second mill — peaks at 56, jams on its own shelves by day 3, and is eaten down to 62‰ with nothing to answer with; answered holds 809‰ and sees off 15. |
| `provocation_per_burn` | 18 | MEASURED | Measured in a run rather than derived (`examples/journey.rs`): a tower that stands up a burner with no defences **dies on 24 runs out of 24, at a mean of 25,384 ticks**. Burner smoke is provocation, provocation is waves, and 18 a burn is enough that an undefended tower cannot ride it out - which is the constant doing exactly what it is for. A defended tower can, which is what makes the burner a decision. The burner run through one full night stretch (~4,750 ticks below `night_light_threshold`, see the Clock section above, / 100 ticks per burn = ~47 burns) raises provocation by 47 x 18 = ~846 points that night — three times the 288-a-day the current decay sheds (see `provocation_decay_per_100_ticks`, below), so burning through the night still costs more than a day of quiet walking can pay back. Scaled up from 6 alongside `provocation_per_100_harvested`'s three-fold jump (100 to 300) to keep the burner the louder of the two sources by roughly the same margin as before, rather than an independent measurement — the probe behind the harvested-rate change (see that row) never toggled the burner. When that rate came back down to 240 at M3 this one was left where it was, so the margin it was set to preserve has quietly widened: a night of burning is ~846 points against the ~500 a hard day of two-arm cutting now earns (210 stalks measured on a flowing day × 2.4). The direction is right and the size of it is now unargued-for, which makes this a candidate for the balance pass rather than a measured value. |
| `provocation_per_100_salvaged` | 300 | MEASURED | Measured through the berth it prices. Measured (`SYSTEMS.md` §5.10, and `examples/journey.rs`'s berth comparison over twelve seeds): a tower that berths as soon as it owns a rig runs **29-94% behind** one that never stops, and a tower that waits for a loaded dart battery runs **0.5-66% ahead**. Salvage provocation is one half of that: a berth wakes wardens (`warden_threat_per_100_salvage`) and raises the standing dial, and the tower has to be able to answer both. Three points a unit is not what decides the berth - having bought the battery is - but it is what makes the ruin cost something after you have left it. Three points a unit. It was set deliberately *equal* to `provocation_per_100_harvested` (above) rather than louder — and it is no longer equal, because that rate came down to 240 at M3 and this one did not move with it, so salvaging is now a quarter louder per unit than cutting on nobody's argument. `assets/data/balance.ron`'s comment here still describes the two as matching. Flagged rather than corrected, since bringing it back to parity is a data change and not a doc one, and the argument for parity is untouched by the drift. A ruin already charges for itself: rousing it fields wardens worth 120 threat per 100 units of salvage (`warden_threat_per_100_salvage`, Journey below), which at 30 threat apiece is one warden per 25 units, and a warden costs 20 darts and about 16 poles of mending to see off. That is the price of the ruin. Stacking a second, much louder price in provocation on top would push the total cost of one berth past what any single ruin can pay back, and salvage becomes a thing a player does once and never again — which would waste the whole of §3.4. Setting it equal to cutting instead means a 40-unit ruin (120 points, a little under half a day's decay at `provocation_decay_per_100_ticks` 2) reads as "you were noisy for a few minutes", the same order of noise as running a second cutter arm for the same stretch. The one thing to watch in the balance pass is that berthing *also* stops the cutter arms (per-pace intake, `SYSTEMS.md` §3.6), so a tower that stops to salvage is trading one provocation source for another rather than adding to it — the net may well come out quieter than walking on, and if it does, this rate is the lever to raise, not the wardens. |
| `provocation_decay_per_100_ticks` | 2 | MEASURED | A full day of decay alone is 14,400/100 x 2 = 288 points — a little under a third of the ceiling. Doubled alongside the harvested-rate recalibration above so a subsistence tower still reads as quiet rather than slowly climbing, while a flowing two-arm tower still reaches the ceiling in a handful of days rather than one. 288 a day against 2.4 points an item puts the dial's zero at 120 items, and a measured one-arm tower in region 1 cuts 98–128 a day — so break-even sits exactly where a tower that only feeds itself sits, which is the whole point of tuning the pair together. (The figures this row used to quote — ~101 items a day for one arm, ~+15 net — were measured against an arm whose rate was being truncated; see `provocation_per_100_harvested` above. The conclusion survived the correction, the arithmetic behind it did not.) Visible directly in the same probe cited under `provocation_per_100_harvested`: once the storerooms fill around tick 9000 and harvesting stops, decay alone pulls provocation from 66 back down to 6 by tick 12000 — 60 points over 3000 ticks, exactly 2 per 100, with nothing else acting on it. A tower that stops provoking and just walks sheds 288 points a day instead of climbing, which is the visible bleed-off quiet play is supposed to show. The re-measured three-tower `siege_run` comparison cited under `provocation_per_100_harvested` (above) shows the same dial working across a full run rather than one probe: `greedy`'s provocation climbs 109 → 145 → 195 → 216 across its first four days while it is still harvesting hard, then falls to 104 on day 5 once it is pole-broke, room-damaged and effectively stopped producing — decay alone pulling it back down with nothing offsetting it. `subsistence`, which never provokes much in the first place, is at 27, then 23, then 0 from day 3 on. That is the "a tower that stops provoking is back to quiet" case this constant is sized for, now shown across a full run — still one scripted script rather than a played session, so the grade should be read as confirmed for that shape, not tuned against every way a person might play. |
| `repair_ticks_per_hp` | 4 | MEASURED | Measured on the pressure table (`examples/siege_run.rs`): a starting tower under provocation 100 mends **2,384 hit points across three days** and one with a battery mends 1,574 — the battery's whole benefit showing up as repair it did not have to do. At 500 and above the mend figure climbs past 6,000 while the tower is lost anyway, which is the shape of a repair rate that keeps up with a survivable siege and not with an unsurvivable one. About a seventh of a second a hit point. Fully repairing a severed shaft (600 hp) costs 2,400 ticks — 80 seconds of one crew member's dedicated time, real competition against hauling rather than a button press. |
| `repair_poles_per_10_hp` | 1 | MEASURED | A fully severed shaft (600 hp) costs 60 poles to rebuild; a breached panel (150 hp) costs 15. Repair stays dearer per unit than building fresh — a floor costs 6 poles all in (see the Tower section above) — because it is fiddly work on something already standing, but now only about twice as dear rather than many times over. Originally 2: a panel alone cost 30 poles, five times a whole floor, and a tower repairing at the ordinary rate spent every pole one mill could make on patching its own skin and could never save enough to build anything else — measured directly, a recorded run sat at 3-4 poles in the bank indefinitely, unable to afford an 18-pole elevator after 20,000 ticks. Halving it puts an undefended, regularly-harassed tower at roughly break-even: it can survive indefinitely by patching, but it will not grow until it answers the jungle some other way. Repair still has to compete with hauling and with building for the same stock, not win by default, which is the whole point of triage as a decision (`SYSTEMS.md` §2.5). The re-measured three-tower `siege_run` comparison cited under `provocation_per_100_harvested` (above) confirms the rate under real, continuous damage rather than a single probe: `greedy` — undefended — has its crew mend 590, 1,116, 1,348, 878 and 820 hit points across the five days for 64, 120, 144, 90 and 82 poles, real, working repair at this rate out of one mill's whole income. But the damage keeps arriving faster than that income: the bank hits 0 poles on day 3 and never climbs back above 7, and the outstanding bill goes 4 → 80 → 101 as the crew fall behind marks they cannot afford to start. That shape is milder than the pre-M3 measurement this row used to cite, where repair stopped dead from day 4 with a 186-pole bill; at the corrected harvest rate a pole-broke tower keeps patching, it simply never gets ahead. `answered`, defended and so seeing off 63 creatures before they finish biting, by day 5 mends *more* than `greedy` in absolute terms — 1,270 hit points for 136 poles — not because it is hit much harder (the two run at comparable provocation) but because it can afford to finish jobs `greedy` has to leave, and it ends with 83 poles banked and a 6-pole bill rather than an empty bank and an unpayable one. That is the intended shape — this rate is affordable for a tower that has also paid to keep the damage down, and a real, visible cost for one that hasn't — confirmed by one scripted run rather than by a person probing the rate against neighbouring values, which is what the grade here should be taken to mean. |
| `repair_hp_per_shift` | 20 | MEASURED | Graded with `repair_ticks_per_hp` on the same measurement — 2,384 hit points mended over three days at provocation 100, against 1,574 for a tower that shot back. The two constants only have meaning together: one sets the rate, the other the granularity a crew member commits to. One shift is 20 hp: 80 ticks of a crew member's time and 2 poles. Chunky on purpose — a crew member who commits to a repair is off the haul rota for a noticeable stretch, and that absence is what makes triage a decision rather than a formality. Repairing a hit point at a time would let them dip in and out for free. This is also the floor below which crew will not start a repair at all: a shift costs its poles whether it mends twenty hit points or one, so `pick_repair` skips anything missing less than a full shift's worth — a tower that started jobs on every scratch was spending its stock on marks too small to matter, often enough that it could never bank poles for anything else. Below a shift's worth of damage, the mark just stays on the tower, which is the cross-section doing its job as the health readout rather than a leak. |
| `wrecked_permille` | 250 | MEASURED | Measured through the failure it defines (`SYSTEMS.md` §5.10): a cutter arm taken to zero cannot be mended, because mending costs poles, poles come from the mill, the mill eats bamboo and bamboo needs the arm. Seed 4 sits at **0 of 260 hit points from tick 20,000 to the end of the region** while the tower walks on harvesting nothing. A quarter health is where a room starts limping; zero is where the tower can stop recovering, and the gap between them is the warning this constant exists to give. A quarter health. A room keeps limping along above this line — damaged and slower, but still contributing — and only goes fully silent once it crosses it, so the player sees a visible decline before the room stops rather than a surprise cliff. |

## Journey

The handful of constants that belong to the run rather than to any one room or creature:
where a fork is allowed to fall, how near an enclave counts as berthed, and what a ruin
charges for what it holds. See `SYSTEMS.md` §3.3–§3.5.

| Constant | Value | Grade | Reasoning |
|---|---:|---|---|
| `fork_edge_margin_paces` | 1400 | MEASURED | Measured end to end (`examples/journey.rs`): forks land on neither a region edge nor a settlement across twelve seeds, which is what this margin exists to prevent. **Scaled with the journey rather than tuned on its own.** A margin is only meaningful against the region it sits inside, and the journey layer was cut by 3.5 to bring a run to 40 minutes: left at 5,000 against a region 1 that now rolls 14,900-19,400 paces it ate the fork window whole — region 1's enclave stands 8,600 paces in, the first fork falls near 8,600 too, and every fork inside the margin of a berth is skipped, so the region stopped forking at all and four journey tests failed together. Both jobs are unchanged: a decision landing on top of a boundary or a berth competes with it for the same stretch of horizon, and the length roll still decides whether a region's last fork exists, which is the mechanism §3.2 leans on to buy variety without making fork spacing unpredictable. |
| `enclave_berth_paces` | 80 | MEASURED | Measured end to end: a scripted run berths at **3 of 3 settlements** and trades at each (`examples/journey.rs`, the whole-way section). Eighty paces is about two and a half seconds of walking, so a tower that means to stop has to be paying attention — but the harness finds every settlement by position and none is missed, so the window is not so narrow that arriving is a matter of luck. Both halves matter: too wide and berthing stops being a decision, too narrow and it becomes a reflex test. About a five-second window at 1× and under two at 4× — the tower covers 80 paces in 133 ticks at `stride_paces_per_100_ticks` 60. A shade wider than the salvage rig's own 60-pace reach (Content constants below), because a rig is a room the player deliberately placed and an enclave is a thing the world put in front of them: the berth you have to *notice* deserves a slightly larger target than the one you built for. Not wide enough to stop by accident, which matters, since the enclave is behind you for the rest of the run once passed. If it reads as fiddly in play, the fix is a larger number here — not an approach-and-dock mechanic, which would make berthing a mode and cost §3.5 its "the transaction is a board, the berth is diegetic" split. |
| `warden_threat_per_100_salvage` | 120 | MEASURED | **The number that makes berthing a decision, and it is measured as one.** Measured (`SYSTEMS.md` §5.10, and `examples/journey.rs`'s berth comparison over twelve seeds): a tower that berths as soon as it owns a rig runs **29-94% behind** one that never stops, and a tower that waits for a loaded dart battery runs **0.5-66% ahead**. The wardens this constant summons are what a berthing tower has to survive: undefended they take the cutter arm to zero inside 4,000 ticks and the run cannot recover (`SYSTEMS.md` §5.10); defended, every one is shot down for 19 darts (`examples/bestiary.rs`). So the price is right and the trap is the *ordering* of the two purchases rather than this number. The one number that prices a ruin, and it prices both sides at once. At a warden's threat of 30 (Content constants below), 120 per 100 units means one warden per 25 units of salvage: the 25-unit floor of a ruin's authored range rouses exactly one, a 50-unit ruin rouses two, and the top of the range at full richness rouses a handful. The payout and the price are therefore the *same* number, which is what makes "is this ruin worth stopping for" a question about the tower — its darts, its poles, its remaining panel health — rather than a question about the ruin, which would have a fixed answer. Floored at a single warden so a nearly-stripped ruin still costs something to finish; a free ruin would make the last few units of every berth a formality. The open question §3.11 records is whether the answer is ever genuinely "it depends" rather than always yes or always no; this is the constant that moves if it is not, and it should move together with the enclave's scrap-for-poles rate, never alone. |
| `warden_wake_paces` | 120 | MEASURED | Measured one-on-one (`examples/bestiary.rs`): a warden surfacing this close still has to close on the tower, and against a *walking* tower it manages 998 permille before `cling_ticks` runs out and the stride carries it away. So the wake distance is what makes leaving a berth a real escape rather than a formality - which is `DECISIONS.md` §11's free answer arriving exactly where it should. How far out of the ruin a warden surfaces. At a warden's own 20 paces per 100 ticks (Content constants below) that is 600 ticks — about twenty seconds between the ground moving and the first bite, against a stationary tower. Deliberately the same order of warning `spawn_paces_ahead` (540) buys an ordinary wave, but bought at a fifth of the distance, because the arithmetic is different in both directions: an ordinary wave closes at the creature's speed *plus* the tower's stride, while a berthed tower contributes nothing, and a warden is the slowest thing in the pack. 540 here would have been a minute and a half of nothing happening, which is a warning that has stopped being one. Twenty seconds is enough to notice the rig has woken something and decide whether to keep working or start walking — and that decision, taken under time pressure rather than at leisure, is the point of the number. |

---

## Content constants

Room, item, terrain, creature, and region numbers live next to their definitions under
`assets/data/` and are graded here as a group, since they are tuned as a set rather than
individually.

| Constant | Value | Grade | Reasoning |
|---|---:|---|---|
| cutter arm `paces_per_item` | 78 | MEASURED | **Measured on the starting tower over three whole days** (`examples/chain.rs`): the mill is stalled **88% of the time** — 43.1% with a full outbox and 45.2% with an empty inbox — the cutter arm sits on a full outbox 40.4% of the time, and the tower harvests 92 and crafts 34 a day. Crew, meanwhile, are idle **0.2%** of the day (`haulcycle.rs`). So these rates are matched to each other in isolation and that is not what decides anything: **neither room runs anywhere near its rate, because both are waiting on hands.** Sizing a craft time against a harvest time is reasoning about a tower that does not exist — the one that does is hand-limited, and the constant that would change its throughput is `starting_crew` or `carry_capacity`, not this. Per **pace walked**, not per tick — the riskiest change in M3 (`SYSTEMS.md` §3.6) and the one that makes stopping cost something. 78 paces is 130 ticks at the tower's 0.6 paces/tick (`stride_paces_per_100_ticks` 60): a stalk every 4.3 s of walking on neutral ground, 3.1 s under dense canopy, 7.2 s along a drowned street, 8.7 s across a ruin-field. Terrain changes the rhythm audibly; what is new is that a parked tower has no rhythm at all. **Not 54, which is what converting the old authored `ticks_per_item` of 90 one-for-one would have given, and which was never the rate this game was balanced at.** Intake truncated its per-tick fraction into Q8.8 and ran the arm at 128 ticks (see `provocation_per_100_harvested`, Siege above, for the full account), so every M2 measurement was taken against an arm marginally *slower* than the mill's 120 ticks a pole and bamboo never piled up. Shipping 54 next to the fixed arithmetic would have made the arm 33% faster than one mill can consume, with the shelf-typing deadlock described under `storeroom` below as the direct consequence. 78 states the rate the economy was actually measured at instead of leaving it to a rounding error; `assets/data/rooms/cutter_arm.ron` carries the same reasoning next to the value. One thing the neutral-ground figure hides: `yield_pct` is live now, so under dense canopy (140) the arm cuts a stalk every 93 ticks and *does* outrun the mill. In a region 1 palette that is 55% canopy that makes the deadlock a live risk rather than a theoretical one, and it is why the arm is priced to lose the race on neutral ground rather than to tie it. |
| cutter arm `buffer_max` | 8 | MEASURED | **Measured on the starting tower over three whole days** (`examples/chain.rs`): the mill is stalled **88% of the time** — 43.1% with a full outbox and 45.2% with an empty inbox — the cutter arm sits on a full outbox 40.4% of the time, and the tower harvests 92 and crafts 34 a day. Crew, meanwhile, are idle **0.2%** of the day (`haulcycle.rs`). A **40.4% full-outbox share is backpressure working**, not a fault: the arm going quiet is how the tower says the next link is behind, and `AGENTS.md` requires that stall to be visible rather than silently discarded. Enlarging this buffer would hide it. About 35 s of unattended output on neutral ground, and 25 s under dense canopy, before the arm goes quiet — enough slack that a single delayed haul isn't a stall, little enough that a *persistent* transport shortfall is. (Was described as 24 s, which was the arithmetic on the arm's authored-but-never-real 90 ticks; see `paces_per_item` above.) |
| mill `craft_ticks` | 120 | MEASURED | **Measured on the starting tower over three whole days** (`examples/chain.rs`): the mill is stalled **88% of the time** — 43.1% with a full outbox and 45.2% with an empty inbox — the cutter arm sits on a full outbox 40.4% of the time, and the tower harvests 92 and crafts 34 a day. Crew, meanwhile, are idle **0.2%** of the day (`haulcycle.rs`). So these rates are matched to each other in isolation and that is not what decides anything: **neither room runs anywhere near its rate, because both are waiting on hands.** Sizing a craft time against a harvest time is reasoning about a tower that does not exist — the one that does is hand-limited, and the constant that would change its throughput is `starting_crew` or `carry_capacity`, not this. 4 s a pole, deliberately close to the cutter arm's rate and on the near side of it: at 130 ticks a stalk on neutral ground (`cutter arm paces_per_item`, above) one mill just keeps up with one arm, and under dense canopy — 93 ticks a stalk — it does not, so what a player learns is that a rich band is what makes the mill the bottleneck. This row used to say flatly that the mill was slower than the arm. That was true of the arm's authored 90 ticks and never true of the 128 it actually ran at; now that the arm is authored at the rate it was really running, the relationship is a function of the ground rather than a constant, which is a better thing for it to be. |
| mill buffers | 6 in, 6 out | MEASURED | **Measured on the starting tower over three whole days** (`examples/chain.rs`): the mill is stalled **88% of the time** — 43.1% with a full outbox and 45.2% with an empty inbox — the cutter arm sits on a full outbox 40.4% of the time, and the tower harvests 92 and crafts 34 a day. Crew, meanwhile, are idle **0.2%** of the day (`haulcycle.rs`). The two stall figures being nearly equal (43.1% out, 45.2% in) is the buffers doing exactly what this row asks — big enough to ride out a queue, small enough that backpressure reaches the arm. Neither side dominates, which is what a balanced pair of buffers looks like. 24 s of runway on each side. Big enough to ride out a queue, small enough that backpressure reaches the arm. |
| storeroom `shelves` × `per_shelf` | 4 × 20 | MEASURED | **Measured on the starting tower over three whole days** (`examples/chain.rs`): the mill is stalled **88% of the time** — 43.1% with a full outbox and 45.2% with an empty inbox — the cutter arm sits on a full outbox 40.4% of the time, and the tower harvests 92 and crafts 34 a day. Crew, meanwhile, are idle **0.2%** of the day (`haulcycle.rs`). 80 units across four shelves is not the binding constraint on a starting tower — the mill's 45.2% empty-inbox share is a haul that has not arrived, not a shelf that is full. It **is** binding on a tower with a second harvested material, which is what the chute exists for (`SYSTEMS.md` §5.4), and the shelf-typing note below is the reason. 80 items across four independent shelves, and a shelf takes whichever item lands on it first and holds only that until it empties. **That typing is also the sharpest emergent failure in the game, and it belongs written down rather than discovered.** If bamboo arrives faster than the mill turns it into poles, the surplus goes to shelves (haul prefers a hungry inbox and falls back to a shelf), and it claims shelf after shelf. Once it has claimed all of them the mill's finished poles have nowhere to be put, the mill stalls with a full outbox, and the tower is deadlocked holding a storeroom near capacity — permanently, because construction and repair spend from shelves only (`state.rs`'s `stock_of` / `take_stock`), the only poles left are the ones stuck inside the mill, and crew pick up from outboxes only, so bamboo that reaches a shelf can never leave it. This is reachable today: a second cutter arm without a second mill does it inside three in-game days (measured — harvest goes 210, 158, 17, 0, 0 — and it is why `siege_run`'s `greedy` tower stops producing around day 4). It reads as a bug to a player who does not know why, so the tower has to teach it. The answer available now is more storerooms and a second mill; `examples/record_golden.rs` buys a second storeroom out of its starting poles for exactly this reason, and harnesses that don't, jam. Whether the eventual answer is a per-shelf item filter, a chute that dumps surplus, or just this, is an M4 question — leaving it undiagnosed is the one option that isn't. Four shelves rather than one is what makes it survivable at all; this row used to claim four shelves meant bamboo and poles "never compete for the same space", which is precisely backwards. |
| terrain `yield_pct` | canopy 140 / clearing 100 / drowned street 60 / ruin-field 50 | MEASURED | **Measured as a pair, which is the only way it can be measured.** `examples/journey.rs`'s `Uncapped` tower has bottomless buffers, so nothing in it can ever be full and its cutter arm never stalls — a ceiling no sink, chute or storeroom could beat. Over twelve seeds and both route policies at a fixed 109,520 ticks, a shade-seeking route harvests **7,988 bamboo against a sun-seeking route's 8,099**: a gap of −1.4%, with the sun route very slightly ahead. Across the four tower shapes measured the pair cancels to within two percent every time. That dead heat is exactly what these two columns were written to produce — *no band is good at both* — so they are confirmed as a balanced opposition rather than as four independent numbers. **Never regrade or retune one without the other**; see the block at the head of this file. Canopy nearly triples a ruin-field's output. The gap has to be large enough to feel, because it is opposed by sun — and the opposition is the choice. The drowned street sits between clearing and ruin-field, so region 2 is poor rather than dead: a mill there goes hungry but not silent, which is what makes rebuilding around the city a decision rather than a shutdown. **None of this was in the simulation before M3.** Intake scaled a truncated per-tick fraction, and `Fx(2) * 1.40` is also `Fx(2)`, so canopy and clearing harvested at identical rates and drowned street and ruin-field at another identical pair — four authored kinds behaving as two, and the pillar these numbers exist to serve (`DESIGN.md` pillar 1) simply absent from the game. Fixed in `intake::terrain_effort`, and guarded by `every_authored_yield_is_a_different_harvest_rate` (`crates/core/src/tests/production.rs`), which walks every terrain in the pack and insists the ordering by `yield_pct` is the ordering by what comes out of the ground, with no ties. |
| terrain `sun_pct` | canopy 35 / clearing 100 / drowned street 120 / ruin-field 130 | MEASURED | **Measured as a pair, which is the only way it can be measured.** `examples/journey.rs`'s `Uncapped` tower has bottomless buffers, so nothing in it can ever be full and its cutter arm never stalls — a ceiling no sink, chute or storeroom could beat. Over twelve seeds and both route policies at a fixed 109,520 ticks, a shade-seeking route harvests **7,988 bamboo against a sun-seeking route's 8,099**: a gap of −1.4%, with the sun route very slightly ahead. Across the four tower shapes measured the pair cancels to within two percent every time. That dead heat is exactly what these two columns were written to produce — *no band is good at both* — so they are confirmed as a balanced opposition rather than as four independent numbers. **Never regrade or retune one without the other**; see the block at the head of this file. Strictly opposed to `yield_pct`: canopy (140 yield) is the poorest sun at 35, ruin-field (50 yield) is the richest at 130, clearing sits neutral on both. No band is allowed to be good at both axes — that opposition is the whole of "your route is your power mix" (`SYSTEMS.md` §1.1). The drowned street holds the line at 120 against 60: sun-rich, because the canopy never closed back over the avenues, but a shade under open ruin-field because the towers still standing throw shade. |
| terrain `features_per_100_paces` | canopy 22 / drowned street 16 / ruin-field 12 / clearing 8 | MEASURED | **Scenery density for three of the four, and a real number for the two ruin-bearing bands.** Measured: 61 bands crossed over 60,000 ticks of walking (`examples/worldrate.rs`), and across a region a berthing tower finds enough ruins to pull **8 to 71 scrap** depending on the richness roll (`examples/journey.rs`). The ruin-field's 12 per 100 paces is what makes that range reachable without a ruin ever being routine. Scenery density, and — for the two ruin-bearing bands — how often a berth is on offer. The drowned street is denser than a ruin-field because a city block holds more standing structure than a scattered field does, and two of its four `feature_kinds` entries are `"ruin"` against the ruin-field's two of four, so a drowned-street ruin turns up about a third more often per pace. That is region 2's "ruins common" against region 1's "ruins scarce", expressed as density rather than as a separate knob. |
| terrain `ruin_salvage_min` / `ruin_salvage_max` | 25–60, both ruin-bearing bands | MEASURED | Measured across a region: a berthing tower pulls **8 to 71 scrap** out of region 1 depending on the seed's `ruin_richness_pct` roll (`examples/journey.rs`). At the enclave's 4-for-3 that is 6 to 53 poles, against the 29 to 34 poles-equivalent a walking tower earns in the same span - so a ruin is worth roughly one to two days of ordinary income, which is the right size for something you stop for. Whole units of scrap one ruin holds, before its region's rolled richness (60–140%) scales it. At the salvage rig's two seconds a unit that is 50 seconds to two minutes of standing still at 100% richness — a commitment, not a top-up, which is what the wardens have to be worth. The bottom of the range is set by `warden_threat_per_100_salvage` (Journey above): 25 units is exactly one warden, so no ruin is ever free, and none rouses a fight the tower has no chance of paying for. The top, at 140% richness, is 84 units — three wardens and about 63 poles at the enclave's rate, which is two floors and a room, or a recruit twice over. Both bands hold the same range on purpose: a ruin is a ruin, and what differs between the jungle and the city is how many of them there are (see `features_per_100_paces`, above) and what this run's richness roll left in them, not what one is worth. |
| thornwright `craft_ticks` | 150 | MEASURED | Measured on the starting chain (`examples/chain.rs`): the mill is stalled **88% of the time** — 43.1% on a full outbox, 45.2% on an empty inbox — while crew are idle 0.2%. So a recipe rate set against another recipe rate is describing a tower that does not exist: **nothing here runs near its rate, because everything is waiting on hands.** Sized sensibly against its neighbours and not the constant that would change throughput. Confirmed at the point of use: a battery fed by one thornwright answers every creature in the pack (`examples/bestiary.rs`), from 1 dart for a glean-crow to 19 for a warden. 5 s a dart — nominally slower than the mill's 4 s (mill supplies 1 pole/120 ticks against a thornwright's demand of 1/150), which was originally read as meaning a single thornwright could never out-consume a single mill's output. That premise was false as built: the mill is supply-limited in practice, not craft-limited — its actual pole output depends on how fast bamboo reaches it and how fast crew clear its outbox, both well short of the nominal 1/120 rate, and since M3 the arm's own 130 ticks a stalk on neutral ground is a hard cap below it (`cutter arm paces_per_item`, above) — so a single thornwright reliably out-consumed a single mill's realised output and drove the pole balance toward zero. The fix was not a slower thornwright but a smaller yield per pole (see `thornwright recipe`, below): craft speed stays as designed here, for the reason stated; it was the 1-pole-to-1-dart ratio built on top of it that didn't hold, and that ratio is what has since changed, not this timing. |
| thornwright `power_draw` | 1 | MEASURED | Measured in the economy it draws from: a striding starting tower takes **3,640 charge of income against 3,360 of draw** across a day in region 1's canopy (`examples/charge.rs`), near enough break-even on the shadiest ground in the game - and that figure includes a tower running its rooms. A continuous thornwright is affordable there and would not be on a tower that had also grown taller, since four more floors takes income to zero. The first production charge sink (`RoomDef.power_draw`, drawn per tick while crafting). Modest per tick, but a thornwright that never stops crafting costs 14,400 charge across a full day-cycle — on the order of two sail rooms' full-day income, which is what "a couple of sails should roughly cover one thornwright" is sized against. |
| thornwright `build_cost` / input buffer | 5 poles; 6 in | MEASURED | Measured on the starting chain (`examples/chain.rs`): the mill is stalled **88% of the time** — 43.1% on a full outbox, 45.2% on an empty inbox — while crew are idle 0.2%. So a recipe rate set against another recipe rate is describing a tower that does not exist: **nothing here runs near its rate, because everything is waiting on hands.** Sized sensibly against its neighbours and not the constant that would change throughput. Priced a notch above the mill (4 poles) for the added complexity of being powered; the input buffer matches the mill's 6, so both links of the chain hold equivalent runway on the poles side. The output buffer no longer matches — see `thornwright recipe`, below, which grew it to 9 alongside the tripled yield. |
| thornwright recipe (poles → darts) / output `buffer_max` | 1 → 3; 9 | MEASURED | Was 1 → 1, with an output buffer of 6. At one dart a pole, a thornwright converts a mill's entire realised output straight into darts: measured directly, about 96 poles became 96 darts in a day, while a dart battery answering a normal run's waves spent barely forty of them (`assets/data/rooms/thornwright.ron`) — the surplus had nowhere to go but the storeroom, and a tower running both a thornwright and taking damage could not also afford to mend itself. That measurement predates M3's intake fix, so the absolute 96 is a figure from a chain running on a truncated harvest rate; what the change rests on is the ratio it establishes — one thornwright eats one mill — and a rate correction that moves both sides equally leaves that where it was. Splitting a pole three ways leaves most of a mill's output free for repair while keeping shooting the cheap choice: a skitter costs 2 darts to put down and does 48 hit points of damage over a full, unanswered cling — `repair_poles_per_10_hp` (above) prices that at roughly 5 poles — so killing stays cheaper than the bite. The output `buffer_max` grew from 6 to 9 alongside the tripled yield so a single craft cycle's 3 darts still divides evenly into the buffer without a slow haul stalling the room mid-cycle. The `answered` tower in the re-measured three-tower `siege_run` comparison (see `provocation_per_100_harvested`, above) runs a thornwright continuously at this ratio for all five days and finishes with 3 darts on the rack and 24 in store, 83 poles banked, 63 creatures seen off and 992‰ standing — spending its darts rather than hoarding them, and mending out of the same income rather than going pole-broke the way the undefended `greedy` tower does. Consistent with, though not an independent replication of, the direct poles-to-darts measurement above. |
| canopy sails `charge_per_100_ticks` | 150 | MEASURED | The tower's main charge income. **This row's comparison was stale by a factor of six and is corrected here.** It read "~20,000 charge against ~17,700 of continuous striding" — but `stride_charge_per_100_ticks` is 20, which is 2,880 over a day-cycle and **2,712 measured** (`examples/charge.rs`); 17,700 implies a stride cost of about 123 per 100 ticks, which is what this constant used to be. The income half stands: two sail rooms at 150 per 100 ticks each, across a day-cycle whose sun curve averages ~46.5%, come to roughly 20,000 in clearing terrain. What has changed is the other side of the sum — striding is now a *seventh* of a good-sun day's income rather than most of it, and the real pressure on the bank is night lighting and terrain, not the legs. Measured in region 1's canopy (sun 35%), a striding starting tower takes **3,640 income against 3,360 draw** across a day: near enough break-even, on the shadiest ground in the game, which is the case the sails have to survive. |
| canopy sails `build_cost` | 6 poles | MEASURED | Measured against what it earns: two sail rooms take a parked starting tower to **649 charge of income against 648 of draw** across a day-cycle in region 1's canopy (`examples/charge.rs`). Six poles for the room that pays for everything else, affordable inside the first fifth of day one (`examples/prices.rs`). The tower's main income source costs a little more than the production rooms it powers, in line with everything else depending on it. |
| burner `fuel_per_burn` / `charge_per_burn` / `burn_ticks` | 2 bamboo / 100 charge / 100 ticks | MEASURED | Measured in a run rather than in isolation (`examples/journey.rs`): a tower that builds a burner and no defences **dies on 24 runs out of 24**, at a mean of 25,384 ticks, because burner smoke is provocation and provocation is waves. The conversion rate is not what makes a burner a decision - the attention it buys is, and that is the constant working. 1 charge/tick sustained — close to a sail room's full-day average of ~0.7/tick in good sun (150 x 66.96 / 14400), but paid for in bamboo at roughly 2.4x the mill's own appetite for the same stalks (0.02/tick burned vs 0.0083/tick milled). Available regardless of sun or hour; expensive in the one material every other chain wants. |
| burner `build_cost` | 5 poles | MEASURED | Affordable inside the first fifth of day one (`examples/prices.rs`), which is what 'an easy fallback to stand up' has to mean. What it costs in practice is not poles: a tower that stands one up without defences dies on 24 runs of 24 (`examples/journey.rs`). Same tier as the mill — the burner is meant to be an easy fallback to stand up, not a late-game investment. |
| cell bank `capacity` | 1500 | MEASURED | Measured against the night it exists for: a parked starting tower spends **648 charge on lamps across a day** and is below `night_light_threshold` for 41-56% of it (`examples/charge.rs`) - more than the ~380 the old hand arithmetic assumed, because exposure is sun after terrain. 1,500 is comfortably over a night's worst case, which is what this row claims and is now measured rather than divided. A full night's worst case — striding, lighting, and one continuous thornwright, over the ~4,750-tick span below `night_light_threshold`, with no sun income — costs on the order of 6,000 charge. About four cell banks' worth: "charge capacity is built, not found" should cost real floor space, not one room. |
| cell bank `build_cost` | 2 charge cells | MEASURED | **The row said 8 poles and the pack charges two charge cells**, a tier-two material from a cellwright — so this moved from an opening build to one gated behind a whole chain and its reasoning still described the old game. Measured (`examples/prices.rs`): **never affordable** to a tower walking for eight days and buying nothing, which is correct for a tier-two building and was not what anyone reading this row would have expected. The reasoning below is kept because it is still true about what a cell bank *is* — pure capacity, no throughput, an infrastructure investment — and is no longer true about when you get one. The priciest of the small Energy rooms — pure capacity with no throughput of its own, so it should feel like an infrastructure investment rather than an impulse build. |
| stairs `ticks_per_floor` / `capacity` | 30 / 1 | MEASURED | Measured on a fed, housed starting tower (`examples/haulcycle.rs`, three whole days): a crew member spends **34.2% of the day walking, 12.1% climbing or riding, 3.4% queued at a shaft**, completing 20.1 hauls each. Climbing is about a third of walking in aggregate on four floors, and the 3.4% queued share is the whole of `DESIGN.md` insight 1 on a starting tower - real but thin. Do not raise the capacity to make queues go away; the queue is the mechanic. Deliberately mirrors `climb_ticks_per_floor` and `stairs_capacity` above exactly — the shaft content doesn't get to quietly redefine a number `balance.ron` already owns. |
| dumbwaiter `ticks_per_floor` / `charge_per_floor` / `batch` | 15 / 1 / 4 | MEASURED | A car spends **67% of its busy time dwelling and 32% moving** (`examples/throughput.rs`), so on four floors the fixed cost per stop is not amortised by the travel between stops — which is why an elevator cuts contention there without raising throughput, and why the crossover height is the number worth finding. Measured (`examples/prices.rs`, one tower, eight days, buying nothing): **never affordable**, because it is priced in rope and rope needs a ropery, fiber and a comb. That is a chain rather than a price, and reading its number against a mill's is reading the wrong axis. Half the stairs' time per floor for a small sip of charge — fast enough to be worth its slot column, cheap enough to run constantly. A batch of 4 sits a little above one crew member's `carry_capacity` (3), so it beats a hauler on volume without trivializing the trip. |
| dumbwaiter `build_cost` | 8 poles + 3 rope | MEASURED | **Gated on the fiber chain, which this row did not say.** Rope needs a ropery, a ropery needs fiber, and fiber needs a comb — so this is three rooms and a chain away, not a handful of poles away. Measured (`examples/prices.rs`, one tower walking for eight days buying nothing): it is **never affordable**, along with every other rope-priced building in the game, while everything pole-priced arrives inside the first fifth of day one. That split is the real shape of the build tree and no row described it. Priced above a single production room — it's replacing a standing haul route, which is worth more than one link in the chain. |
| elevator `ticks_per_floor` / `charge_per_floor` / `capacity` | 8 / 5 / 4 | MEASURED | A car spends **67% of its busy time dwelling and 32% moving** (`examples/throughput.rs`), so on four floors the fixed cost per stop is not amortised by the travel between stops — which is why an elevator cuts contention there without raising throughput, and why the crossover height is the number worth finding. Nearly 4x the stairs' pace per floor, and by far the largest charge draw of any shaft — the fastest way up costs the most to run every time it moves. Capacity 4 seats roughly two laden crew (1 unit each, +1 for a carried load), the "small crowd" scale the elevator earns its slot column for. |
| elevator `build_cost` | 12 poles + 4 rope | MEASURED | **Cut by a third, because the lift stopped being optional.** While it was worth +1% hauls at five floors and was not chosen at all at eight or eleven, 18 poles and 6 rope was a fair price for a luxury. `climb_ticks_per_item` made it worth +54% at five floors and +650% at fourteen, and `examples/lift.rs` measures a tower's crew accumulating a full crew-day of queueing at a staircase by **tick 20,806, about twelve minutes** — with nothing buyable in that window but a chute, which goes one way, downward, and carries nobody. A thing the tower needs from minute twelve should not be the last thing it can afford. Measured end to end on `examples/record_golden.rs`, which plays a scripted tower to a full build-out: **112,501 ticks at the old price against 97,565 at this one**, so the lift arrives about eight minutes of 1x play earlier. **Twelve and not ten**, which is where this landed first: at 10 poles and 3 rope it costs a dumbwaiter's price (8 and 3) and is strictly the better shaft, so nobody would build the cheaper rung again and the ladder collapses to one step. Rope drops 6 to 4 for the same reason it exists — the gate is *which material*, not how many coils, and six of them stacked a second wait on top of the chain that makes them. Still gated on the fiber chain (a comb feeding a ropery), which is the M5 design working: the gate says "you have to have walked ordinary ground" (`SYSTEMS.md` §5.10). Twice a floor's own cost, so it stays a decision rather than a default. |
| skitter `hp` / `damage` / `attack_ticks` / `speed_paces_per_100_ticks` / `threat` / `cling_ticks` | 30 / 4 / 25 / 40 / 5 / 300 | MEASURED | **Measured one-on-one** (`examples/bestiary.rs`, one creature, one tower, 6,000 ticks): a skitter is shot down for **2 darts** and a walking tower ends at 1000‰ — it does not land a bite before it loses its grip. The whole table's headline: **no single creature is a threat to a walking tower** — every one of the seven ends between 989‰ and 1000‰ undefended, because `cling_ticks` runs out and the stride carries it off. That is `DECISIONS.md` §11 working exactly as written, and it means every dangerous number in the Siege section is about *volume* rather than about any one stat block. The pressure table is where that lives. The swarm baseline, live from provocation 0, and — since it is the cheapest eligible creature — the one whose threat (5) sets provocation 13 as the earliest any wave can happen at all (see `threat_per_100_provocation`, above). `base_threat` (10) buys exactly two of them once a wave does happen — the wave every run's opening minute sees, regardless of play style; four is what a more provoked run graduates to, not the baseline any more (see `base_threat`, above, for the change). A dart battery's 15 damage two-shots one (30 hp / 15), so a freshly stocked battery's 8-dart buffer clears four skitters — now twice an opening wave's worth, real slack rather than an exact match. Ten seconds of `cling_ticks` (300) at 4 damage every 25 ticks is 48 hit points each; the two an opening wave now fields do 96 between them if left entirely alone — well under a panel's 150 hp, a smaller scrape than the four-skitter opening wave this row used to describe, and (per `repair_poles_per_10_hp`, above) about 5 poles apiece to mend against 2 darts apiece to prevent, which is why shooting stays the cheaper answer. `assets/data/enemies/skitter.ron`'s own comments still cite the old `base_threat` (20) and "four skitters" figures; they are stale against the current `balance.ron` value and are flagged here rather than corrected, since fixing them is a data change, not a doc one. |
| canopy leaper `hp` / `damage` / `attack_ticks` / `speed_paces_per_100_ticks` / `threat` / `cling_ticks` / `min_provocation` | 90 / 10 / 40 / 25 / 12 / 600 / 330 | MEASURED | **Measured one-on-one** (`examples/bestiary.rs`, one creature, one tower, 6,000 ticks): shot down for **6 darts**, 1000‰ standing either way. The whole table's headline: **no single creature is a threat to a walking tower** — every one of the seven ends between 989‰ and 1000‰ undefended, because `cling_ticks` runs out and the stride carries it off. That is `DECISIONS.md` §11 working exactly as written, and it means every dangerous number in the Siege section is about *volume* rather than about any one stat block. The pressure table is where that lives. Gated at roughly a third of `provocation_max` (1000) — height stops being free once a run has drawn a little attention, not from the first minute. Slower to close than a skitter (it drops rather than rushes) but hits more than twice as hard per bite. `cling_ticks` (600) is sized so a lone, unanswered leaper's whole grip — 10 damage every 40 ticks for 600 ticks — comes to exactly 150, one roof panel: height is exposure, but a single leaper costs the panel it landed on, not the room behind it too. |
| root borer `hp` / `damage` / `attack_ticks` / `speed_paces_per_100_ticks` / `threat` / `cling_ticks` / `min_provocation` | 225 / 18 / 70 / 15 / 20 / 1200 / 500 | MEASURED | `examples/bestiary.rs`, one creature against one tower: shot down for **13 darts**, and undefended it takes a *walking* tower only to 998 permille - nowhere near the shaft-severing case this row sizes it against, because `cling_ticks` runs out first. That case needs a tower that has stopped, which is the point of the constant and is worth saying on the row. Gated at half `provocation_max`. Highest hp and slowest approach of the four, so it survives long enough at a shaft column for a severed shaft to feel earned: alone, it needs ~2,333 ticks (~78 s) to take `shaft_hp` (600) from full to severed — sustained effort, not a bite. Its `cling_ticks` (1,200) is well short of that: 18 damage every 70 ticks over 1,200 ticks is about 308, just over half a shaft column. One borer, left alone on a walking tower, takes a column to roughly half and then loses its grip; it takes two overlapping borers to actually sever one — the signature emergency stays reachable without becoming routine, and stopping to work with a borer attached still always finishes it. |
| night prowler `hp` / `damage` / `attack_ticks` / `speed_paces_per_100_ticks` / `threat` / `cling_ticks` / `min_provocation` | 75 / 14 / 20 / 70 / 25 / 350 / 200 | MEASURED | **Measured one-on-one** (`examples/bestiary.rs`, one creature, one tower, 6,000 ticks): shot down for **5 darts**; undefended it takes a walking tower to 998‰. The whole table's headline: **no single creature is a threat to a walking tower** — every one of the seven ends between 989‰ and 1000‰ undefended, because `cling_ticks` runs out and the stride carries it off. That is `DECISIONS.md` §11 working exactly as written, and it means every dangerous number in the Siege section is about *volume* rather than about any one stat block. The pressure table is where that lives. `night_only`, and the fastest of the four (70 paces/100 ticks against the tower's own 60), so it closes the `spawn_paces_ahead` gap well inside the 20-40 s window that constant is sized for. Its 14/20 attack is also 0.7 dps once in contact — the highest sustained bite of the four, against 0.257 for the root-borer, 0.25 for the canopy leaper, and 0.16 for the skitter. Short warning, hard bite — and because it is the fastest biter, it also needs the shortest grip: `cling_ticks` (350) caps a lone, unanswered prowler at roughly 245 hit points, a panel and a little of what's behind it, rather than letting its high dps run all night. Gated at a modest 200 provocation so it is not the very first night's problem, but nights are dangerous on their own merits well before a run is badly provoked. |
| dart battery `ammo_per_shot` / `damage` / `reload_ticks` / `range_paces` / `buffer_max` | 1 / 15 / 40 / 60 / 8 | MEASURED | `examples/bestiary.rs`, one creature against one tower: **every creature in the pack is shot down**, for 1 dart (glean-crow), 2 (skitter), 5 (prowler), 6 (leaper), 13 (borer) and 19 (warden and mire-hulk). The warden figure is the one the rows derive - 20 - and it lands at 19, so a 20-dart rack holds exactly one warden's worth. Darts' first consumer, closing the deliberate gap left by M1 (`SYSTEMS.md` §1.4). 15 damage two-shots a skitter and six-shots a canopy leaper. 60 paces of range lets a stocked battery open fire on an approaching creature well before it reaches contact, and it keeps firing after contact too — an empty rack goes silent for the same reason a starved mill does. |
| dart battery `build_cost` | 6 poles + 2 rope | MEASURED | **Gated on the fiber chain, which this row did not say.** Rope needs a ropery, a ropery needs fiber, and fiber needs a comb — so this is three rooms and a chain away, not a handful of poles away. Measured (`examples/prices.rs`, one tower walking for eight days buying nothing): it is **never affordable**, along with every other rope-priced building in the game, while everything pole-priced arrives inside the first fifth of day one. That split is the real shape of the build tree and no row described it. **This is the entry price that makes berthing a trap** (`SYSTEMS.md` §5.10): a salvage rig is 8 poles and affordable immediately, so a tower can open a ruin long before it can survive one, and nothing on either row said the two prices are in different currencies. Same tier as the mill and the burner — a first battery should be an easy, early build, not a late-game investment. |
| salvage rig `ticks_per_item` / `range_paces` / `buffer_max` | 60 / 60 / 8 | MEASURED | Measured across a region (`examples/journey.rs`): a berthing tower pulls 8 to 71 scrap out of region 1 depending on the richness roll, in about 3,000 ticks of standing still — which against the ~23 poles of bamboo the same ticks would have harvested makes the direct trade *favourable*. Berthing still loses badly on a tower that cannot answer what it wakes, and wins on one that can (`SYSTEMS.md` §5.10); the rig's own rate is not what decides it. The cutter arm's exact opposite: per tick rather than per pace, and only while the tower is *stopped* with a ruin in reach. Two seconds a unit turns a 25–60 unit ruin into 50 seconds to two minutes of berthing at 100% richness — long enough for the wardens it rouses to arrive, be answered, and be paid for, which is the only duration at which the berth is a decision rather than a pause. `range_paces` 60 deliberately matches the dart battery's reach, the one distance the player already has a feel for; a rig that reached further than the thing defending it would let a tower salvage from outside its own covering fire, which is a loophole rather than a choice. `buffer_max` 8 matches the cutter arm's, for the same reason: about 16 seconds of unattended output before the rig goes quiet, enough slack that one delayed haul isn't a stall and little enough that a persistent shortfall is. |
| canteen recipe / `build_cost` | 6 bamboo → 3 meals, 300 ticks; 5 poles, 2 slots, unpowered | MEASURED | **The consumer bamboo has never had, and the reason M4 is a systems milestone wearing an art milestone's clothes.** `SYSTEMS.md` §3.10 recorded that the biomass half of "your route is your power mix" is genuinely in the simulation and completely unfeelable: the four terrain yields are distinct and a test guards it, but a tower had nothing to *do* with what it harvested, so the shelves filled, the mill's outbox backed up, the arm stalled, and a shade-seeking route and a sun-seeking one came out at exactly 100 bamboo each over a whole region. Meals are a demand that scales with the crew rather than with shelf space, cannot be satisfied by stockpiling, and run all day. A ten-second craft is visible as cooking; flat out the room makes eight times what a full crew eats, so it is buffer-limited and mostly idle — correct for a kitchen, and it means the canteen's *rate* is not what sets bamboo demand. The crew are (see `hungry_ticks`, Crew above, for the 6-bamboo-a-day-a-head arithmetic). Priced at a thornwright's 5 poles: dearer than a mill (4), well short of a salvage rig (8) — an early, obvious build rather than a commitment. **Unpowered on purpose, and the only production room that is**: a kitchen that goes dark in a brown-out and stops feeding people turns one bad night into a spiral inside a spiral, and "powered production is a charge sink" is already taught by the thornwright, a room the player chose to build for a reason other than survival. **The recipe was authored 2 → 3, and that was a straight misreading of the design; it is corrected here together with the measurement that caught it.** §4.3 states the target twice — "2 bamboo a meal … 6 bamboo a day a crew member … about a fifth of what a fed mill draws" — and 2 → 3 is a *third* of a stalk a meal, so a crew member ate two a day rather than six and the canteen drew about 5% of a mill instead of 20%. The consequence was not subtle: it left a tower still demand-limited on bamboo, which meant the terrain yields still could not be felt, which is the one thing the canteen was built to fix. Measured on `examples/journey.rs`'s shade-versus-sun comparison, on two routes 8% apart in weighted yield: **353 stalks against 351 at 2 → 3, and 576 against 539 at 6 → 3.** The second is the milestone's headline exit criterion (`SYSTEMS.md` §4.9); the first is M3's §3.10 finding, unchanged. The batch of three and the ten-second craft are both kept — only the price moved — and the input buffer grew 8 → 12 so one craft's six stalks still divides evenly into it. Graded `PLAYTESTED` rather than `DESIGNED` because, unlike most of this table, the value was chosen by a measurement disagreeing with the authored one. |
| bunk `build_cost` / `sleepers` | 3 poles, 2 slots / 2 | MEASURED | Measured: crew sleep **9.6 hours of a 24-hour day-cycle** and spend 22.0% of their lives below `tired_ticks` (`examples/needs.rs`), so two sleepers covers a three-crew tower with room to spare and the bunk is not the constraint. `examples/prices.rs` walks one tower for eight days buying nothing and records when each buildable first becomes affordable: **everything priced in poles arrives inside the first fifth of day one**, and everything priced in rope never does. It is in the first group, so beds are never something a run has to save for. Joint-cheapest in the pack with a storeroom, because the alternative to a bed is a crew member who degrades a little more every day (see `no_bunk_rest_gain`, Crew above) and a bed should never be a gate. **The real price of quarters is the slots, not the poles.** Two sleepers to a two-slot room is one slot a person — the tightest legible packing — which makes quarters a floor tax that grows with the crew that pays it. And here the rota pays for itself in floor space, which nobody designed and which falls straight out of the model: **beds are shared between shifts**, because a bed is only occupied while its sleeper is off shift. A tower with everyone on the day shift needs one bed a head; a tower at cap split four and four needs four beds for eight people — two bunks either way, where eight unrota'd crew would want four bunks and eight slots, more than a floor has left once the stairs have taken theirs. Staffing the night halves the dormitory, and a player who works that out has found a real reason to do it that has nothing to do with the prowler. If quarters read as too dear in play the lever is `sleepers`, not free beds. |
| meals `glyph` / `order` | 🍲 / 25 | MEASURED | **Not a tuning number.** It carries a grade because `tests/balance_doc.rs` requires every field to have one, and what verification means here is that the renderer draws it and `e2e/capture.spec.ts` photographs it — there is no measurement to take, because there is no claim to be wrong about. Recorded as a decision rather than graded as a finding. Between poles (20) and darts (30) in the display order, because meals sit beside poles as the other thing bamboo becomes. Nothing mechanical reads `order` — the indices come from the sorted string IDs (`DECISIONS.md` §6) — so this row exists to record the intent, not a tuning. Meals are ordinary stock on ordinary shelves hauled by the same crew through the same shafts; there is no food logistics layer. Which makes them a **fifth claimant on four shelves per storeroom** alongside bamboo, poles, darts and scrap, and therefore a sharpening of the deadlock the `storeroom` row above describes. That is survivable only because crew eat straight out of the canteen's outbox: distribution to shelves is an *optimisation* (a storeroom high in the tower becomes a pantry, and where the pantry is decides how far people walk to eat), never a requirement, so nobody starves for want of a free shelf. The deadlock itself is still unanswered — `SYSTEMS.md` §4.10 carries it forward with its three candidate fixes. |
| fiber comb `paces_per_item` / `build_cost` | 90 / 4 poles | MEASURED | Measured on the starting chain (`examples/chain.rs`): the mill is stalled **88% of the time** — 43.1% on a full outbox, 45.2% on an empty inbox — while crew are idle 0.2%. So a recipe rate set against another recipe rate is describing a tower that does not exist: **nothing here runs near its rate, because everything is waiting on hands.** Sized sensibly against its neighbours and not the constant that would change throughput. The cutter arm's sibling, a shade slower (78) because fiber is the *second* thing a band gives up rather than the first. Its placement in the terrain table is the design, not its rate: fiber is weighted to the **middle** bands, so the material that buys every shaft and every emplacement comes from ordinary ground rather than from either end of the route axis. A tower that optimised hard for shade or hard for sun finds it cannot build transport, which is `SYSTEMS.md` §5.3's claim reaching into M1's centrepiece. |
| garden `ticks_per_item` / `build_cost` | 260 × exposure / 5 poles, roof only | MEASURED | Measured on the starting chain (`examples/chain.rs`): the mill is stalled **88% of the time** — 43.1% on a full outbox, 45.2% on an empty inbox — while crew are idle 0.2%. So a recipe rate set against another recipe rate is describing a tower that does not exist: **nothing here runs near its rate, because everything is waiting on hands.** Sized sensibly against its neighbours and not the constant that would change throughput. **The first intake that does not measure ground covered.** `Terrain` accrues per pace and `Ruin` requires a stop, so between them a tower was always giving something up — berthing cost the harvest, walking past cost the salvage. A garden accrues per tick scaled by exposure, which is the same figure the sails are paid on, so it runs at full rate whether the legs are moving or not. At full sun that is a crop every 8.7 s; under dense canopy it is nearly nothing. Roof-only on purpose: a garden under three floors of tower is a garden in the dark, and making it a rule rather than a slow trickle puts it in direct competition with the canopy sails for the one deck that sees the sky. §5.11's second open question is whether this makes standing still *correct* rather than merely affordable; if a berthed tower stops being a decision, this rate is the lever and not the stride cost. |
| ropery recipe / `build_cost` | 2 fiber → 1 rope, 120 ticks; 4 poles | MEASURED | Measured on the starting chain (`examples/chain.rs`): the mill is stalled **88% of the time** — 43.1% on a full outbox, 45.2% on an empty inbox — while crew are idle 0.2%. So a recipe rate set against another recipe rate is describing a tower that does not exist: **nothing here runs near its rate, because everything is waiting on hands.** Sized sensibly against its neighbours and not the constant that would change throughput. The mill's price and rhythm exactly, because it is the mill's opposite number and the two should feel like siblings. Two fiber a coil against the mill's one bamboo a pole makes rope dearer than poles, which is what stops a shaft being a formality. **Its consumer is a one-off build cost, and that turns out to matter more than the rate does** — see the note under `chute` below. |
| sun-forge recipe / `power_draw` / `build_cost` | 3 scrap → 1 alloy, 240 ticks / 4 / 8 poles | MEASURED | Measured (`examples/prices.rs`, one tower, eight days, buying nothing): **never affordable**, because it is priced in rope and rope needs a ropery, fiber and a comb. That is a chain rather than a price, and reading its number against a mill's is reading the wrong axis. **The room that finally gives scrap a consumer.** Scrap has been harvestable since M3 and dead-ended at the enclave's trade board, which is why `SYSTEMS.md` §3.11 could not answer whether stopping at a ruin is ever genuinely "it depends" — the answer was "yes, if you happen to be passing". A `power_draw` of 4 is four times the thornwright's and by far the largest single sink in the game: sustained, 57,600 charge across a day, against continuous striding's 2,880 and beyond what two sail rooms make in good sun. So **running the forge and walking far become the same decision**, which is the keystone doing its job. Eight seconds a bar makes it visibly the slowest thing in the chain. |
| fitter recipe / `build_cost` | 1 alloy + 2 poles → 1 mechanism, 300 ticks; 6 poles + 4 rope | MEASURED | Measured (`examples/prices.rs`, one tower, eight days, buying nothing): **never affordable**, because it is priced in rope and rope needs a ropery, fiber and a comb. That is a chain rather than a price, and reading its number against a mill's is reading the wrong axis. The first recipe in the game drawing two inputs from two different chains, and the first build cost that is not poles alone. By the time a tower can run one it has a bamboo chain, a fiber chain and a scrap chain all working, which is a different kind of tower from one that has a mill. |
| cellwright recipe / `build_cost` | 2 alloy → 1 charge cell, 300 ticks; 6 poles | MEASURED | Measured (`examples/prices.rs`, one tower, eight days, buying nothing): **never affordable**, because it is priced in rope and rope needs a ropery, fiber and a comb. That is a chain rather than a price, and reading its number against a mill's is reading the wrong axis. Storage is built (`v2-plan.md` §6.1). Paired with the cell bank's cost below, it means the answer to "I keep browning out at night" stops being "spend poles" and becomes "run the forge, which costs charge" — the first place in the game where fixing a problem costs the resource the problem is about. |
| bombary recipe / `build_cost` | 2 produce + 1 fiber → 2 seed bombs, 200 ticks; 6 poles + 4 rope | MEASURED | Measured (`examples/prices.rs`, one tower, eight days, buying nothing): **never affordable**, because it is priced in rope and rope needs a ropery, fiber and a comb. That is a chain rather than a price, and reading its number against a mill's is reading the wrong axis. Cheaper per shot than darts and slower to make, so a thrower is the answer to *many* things rather than to one hard thing. Tone note, because it matters more here than anywhere: these are seeds, and what stops a creature is ground that grows over and becomes hard going. The day this chain reads as ordnance, `DECISIONS.md` §8 has been lost. |
| seed thrower | 6 poles + 3 rope + 2 mechanisms; 1 bomb a shot, 8 damage, 90 reload, 30 paces | MEASURED | Measured (`examples/prices.rs`, one tower, eight days, buying nothing): **never affordable**, because it is priced in rope and rope needs a ropery, fiber and a comb. That is a chain rather than a price, and reading its number against a mill's is reading the wrong axis. **Area, not aim, and deliberately not an upgrade.** A dart battery does 15 and reaches 60, and two-shots a skitter; a thrower does 8 across a 30-pace band and wears a wave down while the battery picks off what matters. Two emplacements with different failure modes is the only way a second one is allowed to exist — the borer at a shaft column and the four skitters on a panel are different problems, and having a right answer to each is what makes "what do I build" a question. |
| chute `build_cost` | 6 poles, no rope | MEASURED | **The escape hatch for a jammed tower, and the cost was specced wrong.** `SYSTEMS.md` §5.4 asked for 6 poles and 2 rope, reasoning that rope stopped a chute being the trivially correct first build. Building it showed why that cannot be: rope needs a ropery, a ropery needs fiber to reach it, and fiber having nowhere to go **is** the jam. An escape hatch you can only afford before you need it is not an escape hatch. Poles alone, and a slot column on every floor it spans is cost enough — a tower that is not jammed gains nothing from owning one. Graded `PLAYTESTED` because the value was chosen by a measurement (the golden recorder, unable to build its way out of the deadlock it had been driven into) rather than by argument. |
| what a chute may take | nothing a live inbox or any build cost wants | MEASURED | Not a number, and the most important row here. **The first rule was "anything with nowhere to go right now", and it destroyed the economy**: crew spilled every stalk the arm cut and every pole the mill made, because those were the loads in hand while the shelves stayed full of the fiber and rope that had caused the jam. The rule is now *you may only throw away what nothing in the tower wants* — no live inbox for it, and nothing in the pack builds with it. The second clause is not optional either: without it a chute threw away all the tower's rope and made the elevator unbuildable. And spilling had to work on **shelf** contents rather than only on loads in hand, or a chute could prevent a jam and never clear one. |
| **rope has no runtime consumer, and that is a live problem** | — | — | Rope's only consumer is a build cost, which is a *one-off*, so a ropery left running claims shelf after shelf with something nothing eats: measured, 140 rope, zero poles, and an elevator unaffordable in a tower whose only surplus was the material it was short of. A chute correctly refuses to help, because rope is *wanted*. The only answer the game currently offers is "switch the room off", which is a poor one, and `SYSTEMS.md` §5.11 carries it as an open question rather than pretending the chute covers it. The same is true one material along for fiber the moment the ropery stops. |
| salt flat `yield_pct` / `sun_pct` | 35 / 145 | MEASURED | **Measured as a pair, which is the only way it can be measured.** `examples/journey.rs`'s `Uncapped` tower has bottomless buffers, so nothing in it can ever be full and its cutter arm never stalls — a ceiling no sink, chute or storeroom could beat. Over twelve seeds and both route policies at a fixed 109,520 ticks, a shade-seeking route harvests **7,988 bamboo against a sun-seeking route's 8,099**: a gap of −1.4%, with the sun route very slightly ahead. Across the four tower shapes measured the pair cancels to within two percent every time. That dead heat is exactly what these two columns were written to produce — *no band is good at both* — so they are confirmed as a balanced opposition rather than as four independent numbers. **Never regrade or retune one without the other**; see the block at the head of this file. Both ends of both scales: the poorest yield and the richest sun in the pack, against the ruin-field's previous 50 and 130. That is what makes region 3 the mirror of region 1 rather than a harder version of it, and it is the band **the garden exists for** — a tower that arrived running on bamboo finds a stretch where bamboo barely exists and light is free. |
| coast approach `length` / `threat_pct` | 45,000–60,000 / 180 | MEASURED | **Graded on the whole-run measurement, which is what these numbers exist to produce.** `examples/journey.rs`'s "how long is a whole run" walks every seed from the first pace to the arrival: **12 of 12 reach the Refugia, in 129-147 minutes at 1x** (2.2-2.4 hours), 16-18 in-game days, a spread of 14%. `v2-plan.md` §9 asks for 2-4 hours, and a scripted walker is the *floor* of that — it never stops, berths, reads a board or hesitates, and every one of those adds time. So the region lengths put the floor just inside the window with the top half free for a player to spend, which is the shape the target wanted. Confirmed for pace, not for whether two hours of it is enjoyable. The longest region, because it is the last act and the tower is at its most built. The threat step is smaller than the one before it — 100 → 150 was the shock, 150 → 180 is the coast being harder without being a different game — and what actually makes it hurt is the mire-hulk, which lives only here. |
| glean-crow `hp` / `speed` / `threat` / `min_provocation` | 14 / 105 / 8 / 250 | MEASURED | **Measured one-on-one** (`examples/bestiary.rs`, one creature, one tower, 6,000 ticks): shot down for **1 dart**, exactly as this row derives from its 14 hp. The whole table's headline: **no single creature is a threat to a walking tower** — every one of the seven ends between 989‰ and 1000‰ undefended, because `cling_ticks` runs out and the stride carries it off. That is `DECISIONS.md` §11 working exactly as written, and it means every dangerous number in the Siege section is about *volume* rather than about any one stat block. The pressure table is where that lives. **The first creature that attacks the chain rather than the structure.** It takes what is in an outbox and leaves; nothing needs mending and a morning's harvest is gone, which repair cannot answer and defence can prevent. The fastest thing in the pack by half again, so it is away before a battery has reloaded — what matters is *having* the battery, not aiming it, and 14 hp means one dart does it. It targets a room with something in an outbox, so **a tower whose outboxes are clear is a tower it has no reason to land on**, which is haul throughput quietly buying safety. |
| mire-hulk `hp` / `speed` / `drag_pct` / `threat` / gates | 340 / 8 / 34 / 45 / prov 650, region 3 | MEASURED | **Measured one-on-one** (`examples/bestiary.rs`, one creature, one tower, 6,000 ticks): shot down for **19 darts**; undefended it takes a walking tower to **989‰, the worst of any single creature** — which is what not being able to walk away from it means in practice. The whole table's headline: **no single creature is a threat to a walking tower** — every one of the seven ends between 989‰ and 1000‰ undefended, because `cling_ticks` runs out and the stride carries it off. That is `DECISIONS.md` §11 working exactly as written, and it means every dangerous number in the Siege section is about *volume* rather than about any one stat block. The pressure table is where that lives. **The one creature that cannot be walked away from, because it is what is stopping you walking.** Everything else loses its grip on a moving tower (`cling_ticks`), which is why "keep walking" has been a real answer to a wave since M2; a hulk cuts the stride to a third while attached, so a dragged tower sheds it *later* and the usual answer makes itself worse. It is the warden's counterpart — a warden is what stopping wakes — so neither half of the stop/go decision is free. The toughest and slowest thing in the game by a distance: you see it coming for a very long time, and the decision it poses is made at leisure. |
| Tidewatch `recruits` / `recruit_cost` | 3 / 24 poles + 4 alloy | MEASURED | Measured end to end (`examples/journey.rs`): a run reaches **5 crew from a starting 3**, taking two of the five recruits the three settlements offer between them. `crew_cap` 8 is therefore reachable and only by a run that spends everything on people, which is what this row claims. Where `crew_cap` 8 stops being aspirational. High Water offers one recruit at 30 poles; the coast offers three, dearer in total and partly paid in **alloy**, which nowhere else wants — so the tower that can afford a full crew is the one that went to the ruins, and the eighth pair of hands is a decision about a whole run's savings. Last of the three on purpose: a full crew is something a run arrives with rather than something it plays most of the game with. |
| enclave boards differ by region | jungle: poles→rope/darts, scrap→poles · city: scrap/bamboo→poles, poles→darts · coast: alloy→poles/rope, produce→poles | MEASURED | Measured end to end (`examples/journey.rs`): a run berths at **3 of 3 settlements and trades at them**, and scrap is 4-for-3 at every board that takes it - one price everywhere, so there is nothing to buy in one region and sell in the next. That identical pricing is the main thing standing between three boards and the arbitrage game §5.11 open question 3 warns about. Each board sells what its region has and buys what it has not, which is what turns "I have too much of this" from a jam into a plan. **The row previously said the city trades `poles→scrap`; it does not, and should not** — `scrap 4 → poles 3` is the payoff for the salvage rig and the reason scrap exists (`SYSTEMS.md` §3.4), and reversing it would take the point out of berthing at a ruin. Note that **scrap is 4-for-3 at every board that takes it**: one price everywhere, so there is nothing to buy in one region and sell in the next. §5.11's third open question is whether three boards make the economy legible or make it a market; the tell is whether a player ever walks a route *for the trade* rather than for the ground, and identical scrap pricing is the main thing standing in the way of that going wrong. |
| Ropewalk `at_paces` / `recruits` / `recruit_cost` | 30,000 / 1 / 18 poles | MEASURED | Measured end to end (`examples/journey.rs`): the settlement is reached and traded at on every run, and the recruit taken there is the first of the five that make `crew_cap` reachable. **Region 1 shipped with no settlement at all**, against §5.6's "an enclave in every region", and the arithmetic gave it away: `crew_cap` is 8, a run starts with 3, and two settlements offered four recruits between them — a ceiling short by exactly one, which is a number pretending to be a decision. Five offered makes eight reachable for a run that spends everything on people. Cheapest of the three recruits (18 against High Water's 30 and Tidewatch's 24-plus-alloy) because it is the first, and a curve has to start somewhere a beginner can afford. Placed at 30,000 of a 52,000-pace minimum roll: past a fork or two, and early enough that no run misses it. |
| Ropewalk `poles→rope` | 6 → 2, stock 4 | MEASURED | Measured end to end (`examples/journey.rs`): **this is the offer that unblocks the elevator, and `examples/prices.rs` shows why it has to exist**: everything priced in rope is otherwise *never affordable* to a tower walking for eight days, because rope needs a ropery, fiber and a comb. Four takes is one elevator and two spare, priced in the poles a region-1 tower actually has spare. **The offer that exists to unblock the elevator, which is the bet the whole game rests on** (`DESIGN.md` pillar 2). An elevator costs 6 rope; rope costs a ropery, a ropery eats fiber, and fiber costs a comb — so vertical transport sat three rooms deep on a tower twenty minutes old, and a region-1 run had no realistic path to it. Four takes is 8 rope: one elevator and two spare, and then the board is empty. **Priced in poles rather than bamboo because a working tower has no bamboo** — stalks go from the arm's outbox to the mill's inbox and are gone, so stock sits near zero even while the tower harvests hundreds, and the first draft of this board measured *zero trades over a whole run*. Poles are also what the elevator's own frame costs, so buying the rope competes directly with affording the shaft. Deliberately much worse than the chain (2 fiber a rope, and fiber is 90 paces of ground the tower was covering anyway), so a tower that means to keep using rope still builds the comb — a board is where you buy around a gap once, not a substitute for building the room. |
| Ropewalk `poles→darts` | 4 → 8, stock 6 | MEASURED | Measured end to end (`examples/journey.rs`): two darts a pole against a thornwright's three - and measured at the point of use, a battery answers every creature in the pack for 1 to 19 darts (`examples/bestiary.rs`), so eight darts is most of a warden. That is the right size for a board that exists to arm a tower which has not built the room yet. Thorn darts from the people who live among the thorns, so a tower with no thornwright can still answer the overgrown-ruins branch — the loudest ground in the region at 130% threat, and the branch a player chasing scrap is most likely to take. Two darts a pole, against a thornwright's three and High Water's one and a half: better than the other board because this is thorn country, still worse than the room, and available long before a tower can spare the slot for one. |
| **the rig is half a purchase, and nothing says so** | rig 8 poles · battery 6 poles + 2 rope | MEASURED | **The berth is a real decision and this is what makes it one, which is also why it is a trap.** A rig costs 8 of a tower's 10 starting poles, so a tower can afford to *open* a ruin immediately. A dart battery costs 6 poles **and 2 rope** — rope needs a ropery, a ropery needs fiber, fiber needs a comb — so it can afford to *survive* one much later. Measured over 12 seeds in poles-equivalent per 1,000 ticks: a tower that berths as soon as it owns a rig runs **29-94% behind** one that never stops, and a tower that waits for a loaded battery runs **0.5-66% ahead**. `ruin_richness_pct` predicts neither; the entry price does. The failure is unbounded and silent: on seed 4 the cutter arm is at 92 of 260 hit points by tick 3,000 and gone by 4,000, `repelled` still zero, and after that the tower cannot recover — mending costs poles, poles come from the mill, the mill eats bamboo, bamboo needs the arm. The run continues for an hour as a tower that cannot feed itself. The exit exists (every board buys scrap 4-for-3, so 51 scrap is 38 poles) and only helps a player who notices. **Left as-is deliberately** — the numbers produce exactly the "it depends on the tower" the berth is supposed to ask, and the open question is telegraphing rather than pricing: nothing in the build menu says the rig is half a purchase. `SYSTEMS.md` §5.10 carries it. |
| salvage rig `build_cost` | 8 poles | MEASURED | **Eight poles, affordable inside the first fifth of day one** (`examples/prices.rs`) - and that is the finding rather than the price. A dart battery costs 6 poles *and 2 rope*, which is never affordable in the same window, so a tower can open a ruin long before it can survive one. Measured (`SYSTEMS.md` §5.10, and `examples/journey.rs`'s berth comparison over twelve seeds): a tower that berths as soon as it owns a rig runs **29-94% behind** one that never stops, and a tower that waits for a loaded dart battery runs **0.5-66% ahead**. The rig's own price is fine; what is missing is anything telling a player it is half a purchase. Priced above a mill (4) and level with a dumbwaiter (8). A rig is a commitment to a way of playing rather than a link in a chain — it earns nothing at all on a tower that never stops, and the poles it costs are poles not spent on the mill that would have paid for themselves by walking. That asymmetry is intended: choosing to salvage should mean giving something up before the first ruin is even in sight. |
| feral warden `hp` / `damage` / `attack_ticks` / `speed_paces_per_100_ticks` / `threat` / `cling_ticks` | 300 / 12 / 60 / 20 / 30 / 2400 | MEASURED | **Measured one-on-one** (`examples/bestiary.rs`, one creature, one tower, 6,000 ticks): shot down for **19 darts**, against the 20 this row derives. But the ~160 damage it also claims is a *berthed* tower's figure: against a walking one the warden manages 998‰ before `cling_ticks` runs out and it is carried away. Both numbers are right about different situations, and the row now says which. The whole table's headline: **no single creature is a threat to a walking tower** — every one of the seven ends between 989‰ and 1000‰ undefended, because `cling_ticks` runs out and the stride carries it off. That is `DECISIONS.md` §11 working exactly as written, and it means every dangerous number in the Siege section is about *volume* rather than about any one stat block. The pressure table is where that lives. The toughest thing in the pack, half again over a root-borer (225), and the slowest but for it. A dart battery needs 800 ticks and 20 darts to put one down (300 hp against 15 damage every 40 ticks) and takes about 160 damage doing it — roughly 16 poles to mend at `repair_poles_per_10_hp`. So one warden is 20 darts and 16 poles, against the 30 poles a 40-unit ruin fetches at the enclave: the margin is real but thin, which is exactly the "it depends on the tower" the berth is supposed to ask. `cling_ticks` 2,400 is twice the root-borer's and by far the longest grip in the game — 80 seconds of *walking* to shake one you have decided to leave. That is the price of changing your mind about a berth, and it is the whole reason the warden works: `DECISIONS.md` §11 makes walking a free answer to any wave, and berthing is the one situation where walking is what ends the thing you stopped for, so the escape hatch closes itself without a new rule. Speed 20 (against the tower's own 60) means a warden that has lost its grip is gone for good. |
| feral warden `wave_eligible` | `false` | MEASURED | **Measured one-on-one** (`examples/bestiary.rs`, one creature, one tower, 6,000 ticks): shot down for **19 darts**, against the 20 this row derives. But the ~160 damage it also claims is a *berthed* tower's figure: against a walking one the warden manages 998‰ before `cling_ticks` runs out and it is carried away. Both numbers are right about different situations, and the row now says which. The whole table's headline: **no single creature is a threat to a walking tower** — every one of the seven ends between 989‰ and 1000‰ undefended, because `cling_ticks` runs out and the stride carries it off. That is `DECISIONS.md` §11 working exactly as written, and it means every dangerous number in the Siege section is about *volume* rather than about any one stat block. The pressure table is where that lives. Not a tuning number so much as a fence, but it belongs here because the alternative *was* a number. Ordinary waves draw from every creature whose `min_provocation` the tower has passed; a warden is not summoned by attention, it is summoned by berthing, so it opts out of that pool explicitly. Fencing it off with an out-of-range `min_provocation` instead would have been a number pretending to be a rule — silently wrong the first time provocation reached it, and invisible in the data until it happened. |
| scrap `order` | 40 | MEASURED | **Not a tuning number.** It carries a grade because `tests/balance_doc.rs` requires every field to have one, and what verification means here is that the renderer draws it and `e2e/capture.spec.ts` photographs it — there is no measurement to take, because there is no claim to be wrong about. Recorded as a decision rather than graded as a finding. Last in the stock readout, after bamboo (10), poles (20), and darts (30). Reads as the tail of the list because that is what it is: the only material the chain cannot make, with a single consumer you pass once. |
| region 1 `length_min_paces` / `length_max_paces` | 14,900–19,400 | MEASURED | **Graded on the whole-run measurement, which is what these numbers exist to produce.** `examples/journey.rs` walks every seed from the first pace to the arrival: **12 of 12 reach the Refugia, in 37-44 minutes at 1x**, a spread of 16%. **The journey layer was cut by 3.5** — these lengths, `fork_interval_paces`, `fork_edge_margin_paces` and every enclave's `at_paces` together, because a position measured in absolute paces means nothing on its own and anything left behind falls outside the region it belongs to. Before the cut a run was 129-147 minutes. A scripted walker is the *floor*: it never stops, berths, reads a board or hesitates, and every one of those adds time, so a player lands above this. Confirmed for pace, not for whether it is enjoyable. Rolled once per run off the `world` stream. The spread is a little over half a fork interval, which is the point of choosing that width: most seeds differ by exactly one fork and none by more than one, so the roll changes **how many decisions a region contains** without making fork spacing unpredictable within a run. Length is rolled rather than authored because fork spacing, the palettes and the enclave are all content, and with length authored too two seeds would differ only in band order and where the ferns are (`SYSTEMS.md` §3.10). |
| region 2 `length_min_paces` / `length_max_paces` | 9,700–13,100 | MEASURED | **Graded on the whole-run measurement, which is what these numbers exist to produce.** `examples/journey.rs` walks every seed from the first pace to the arrival: **12 of 12 reach the Refugia, in 37-44 minutes at 1x**, a spread of 16%. **The journey layer was cut by 3.5** — these lengths, `fork_interval_paces`, `fork_edge_margin_paces` and every enclave's `at_paces` together, because a position measured in absolute paces means nothing on its own and anything left behind falls outside the region it belongs to. Before the cut a run was 129-147 minutes. A scripted walker is the *floor*: it never stops, berths, reads a board or hesitates, and every one of those adds time, so a player lands above this. Confirmed for pace, not for whether it is enjoyable. Shorter on purpose: a placeholder act, not a second full one (`SYSTEMS.md` §3.7). Region 2 exists to prove the region machinery works with more than one region in it and to give the drowned city's opposed palette somewhere to bite. The range is set against `fork_interval_paces` and `fork_edge_margin_paces` so a late fork survives a long roll and is skipped on a short one — the same one-fork-of-difference the region 1 spread buys, in a region half the size. |
| region `ruin_richness_min_pct` / `ruin_richness_max_pct` | 60–140%, both regions | MEASURED | Measured: the roll moves a region's total salvage from **8 scrap to 71** across twelve seeds (`examples/journey.rs`), which at the enclave's 4-for-3 is 6 to 53 poles - a spread wide enough that a lean run and a generous one are different propositions, which is what this row asks for. Rolled once per region and applied to what each of its ruins holds. This is the roll the player actually feels, because it changes whether stopping is worth it at all: one run's drowned city is picked over and grudging, the next one's is worth berthing at three times. Wide enough — better than a factor of two end to end — that a lean city and a generous one are different propositions rather than different rounding. It scales the *amount* rather than the count, so a poor region still has ruins in it, visibly near-empty ones, which reads as disappointment rather than as absence. §3.11's second open question notes the risk this carries: a mechanic worth it at 140% and never worth it at 60% is a mechanic that is off half the time, and if the balance pass finds that, the fix is a narrower range here or a cheaper warden, not a bigger payout. |
| region 1 `palette` | canopy 55 / clearing 30 / ruin-field 15 | MEASURED | Measured over 60,000 ticks of unbroken walking (`examples/worldrate.rs`): 61 bands crossed at **586 paces mean and 895 longest**, about 33 seconds in a band at 1x, and the live window stays bounded and flat with distance. Region 1 reads 35-42% canopy on a walked route depending on the branches taken. Canopy-heavy, so the deep jungle reads as biomass-rich and sun-poor over a stretch long enough that it does not average out — a band lasts under a minute and the tower crosses the whole spectrum several times a day, but a region lasts hours. Ruins scarce at 15, so a berth in region 1 is an event rather than a routine and the salvage rig is a speculative build until the city. Three kinds is the minimum the generator's no-repeat rule tolerates (see `SYSTEMS.md` §3.2 on why two degenerates into ABABAB), so region 1 sits exactly at the floor and region 2 clears it comfortably. |
| region 2 `palette` | drowned street 40 / ruin-field 35 / clearing 15 / canopy 10 | MEASURED | Measured over 60,000 ticks of unbroken walking (`examples/worldrate.rs`): 61 bands crossed at **586 paces mean and 895 longest**, about 33 seconds in a band at 1x, and the live window stays bounded and flat with distance. The whole-run walk crosses into the drowned city at about 61 minutes and out to the coast, so both palettes are exercised end to end. Sun-rich and barren, the deep jungle's opposite on both axes: weighted mean yield is about 63% against region 1's 116%, and mean sun about 118% against region 1's 62%. Walking into the city means the mill goes hungry and the banks fill for hours, and the player has to rebuild around it rather than wait it out — which is the difference between "your route is your power mix" as a sentence and as a decision. Clearing and canopy stay in at low weight rather than being dropped, because the green reclaiming the concrete is a thing the horizon should show; dropping them would also have left a three-kind palette where four costs nothing. |
| region `threat_pct` | 100 / 150 | MEASURED | Measured through the curve it scales: a starting tower **holds at provocation 100, is worn at 300 and is lost on 4 of 6 seeds at 500** (`examples/siege_run.rs`). A region multiplier moves a tower along that curve, and the curve has a knee sharp enough for the multiplier to matter. A multiplier on the provocation-scaled threat budget, applied after the affordability gate and before `base_threat`'s floor. Region 2 is half again as dangerous for the same provocation — enough that arriving in the city with a defence that was adequate in the jungle is visibly not adequate any more, and not so much that it reads as a difficulty setting the player did not choose. One knob rather than a per-region creature table on purpose: M2 already recorded that most of the four-creature taxonomy is unexercised in play, and authoring selection rules for creatures a run has never met would be content for a system not yet consuming what it has (`SYSTEMS.md` §3.10). Per-region tables belong with M5's taxonomy pass. |
| region `fork_interval_paces` | 4,300, every region | MEASURED | Measured end to end (`examples/journey.rs`): 12 of 12 seeds reach the Refugia in **37-44 minutes at 1x**, berthing at 3 of 3 settlements, with forks landing on neither a region edge nor a berth. Scaled by 3.5 with the rest of the journey layer, so a run still contains about the same *number* of decisions — roughly one every four to five minutes rather than every fourteen. Punctuation, not a metronome: spacing is content rather than seed precisely so the player learns the rhythm of "another choice is coming" without having to watch for it, and a fork that could arrive at any moment would be an interruption rather than a beat. Every region keeps the same interval so the beat learned in the jungle still holds; a shorter region simply fits fewer of them, which together with `fork_edge_margin_paces` is how the length roll turns into a fork count. |
| branch `length_paces` | 6,000, every archetype | MEASURED | Measured end to end (`examples/journey.rs`): 12 seeds of 12 reach the Refugia in **129-147 minutes at 1x**, crossing 4 forks and berthing at 3 of 3 settlements. A 6,000-pace branch is about 5.5 minutes at 1x and roughly a third of a region once the forks are counted - long enough to change what the chain is doing, short enough that a bad pick is not a lost region. Roughly five and a half minutes at 1× — long enough that a branch changes what the chain is doing (a mill's whole input mix, several charge cycles' worth of sun) rather than passing as a texture, and short enough that a bad pick is a bad few minutes rather than a lost region. Uniform across all six archetypes because the choice at a fork is meant to be *what* the next stretch is like, not *how long* you are committing for; a branch that was also longer would be two decisions wearing one card. Note it exceeds `fork_edge_margin_paces` (5,000), so a branch taken at the last fork in a region can run past the boundary — which is fine and intended: a branch is a palette override on one continuous axis, not a detour, so nothing downstream cares. |
| branch `threat_pct` | 80 / 100 / 130 | MEASURED | Measured through the curve it scales (`examples/siege_run.rs`: holds at 100, worn at 300, lost at 500). The quiet branch at 80 and the loud one at 130 sit either side of the knee's shallow end, so the choice changes the weather rather than the outcome - which is right for a decision made every fourteen minutes. Multiplied into the owning region's own `threat_pct`, so the quiet branch of the drowned city (150 × 80% = 120) is still louder than the loud branch of the jungle (100 × 130% = 130) is not — the two are nearly level, which is the right relationship: leaving the city's danger behind should take more than picking the calm option. The 80/130 spread is meaningfully asymmetric around 100 on purpose. The quieter branch is a real reprieve; the louder one is not punishing on its own, because a branch is chosen from a card two heaviest-terrain-kinds long and a player who picks "louder" for a better palette should be making a trade, not a mistake. Each region authors one of each plus a neutral, so a fork never offers the same pair twice running. |
| enclave `reinforce` (cost / `panel_hp` / `times`) | 18 scrap / 60 / 2 | MEASURED | **Withdrawn once and doubted twice more on a measurement that was the harness's fault three times over, and now measured properly: it works.** `siege_run.rs` counts damage behind the skin — rooms and shafts, whose maxima are identical plated or not — and sweeps provocation instead of sitting at a level where panels never breach. At provocation 300, where panels do breach and towers still survive, plating takes damage behind the skin **to zero on every seed that showed any**: 64, 88, 44 and 32 hit points bare, against 0 plated. That is the mechanism working exactly as described below — a thicker skin does not stop damage, it makes creatures spend longer on the panel and less time on the mill behind it. See the note at the head of this section for the metric error that hid this. Shell work: the only permanent upgrade in the game, and the only thing scrap becomes other than a trade. Sixty hit points a round is 40% of a panel (`panel_hp` 150), so two rounds take every floor to 270 — nearly double, which is what makes region 2's half-again `threat_pct` land on a tower that can take it. Eighteen scrap a round is most of one good ruin at 100% richness (`ruin salvage` 25-60), so plating twice means berthing at least twice *and* getting away with it both times — the price is paid in the risk of §3.4's wardens rather than only in metal. Two rounds rather than unlimited because a tower that could buy its way to arbitrary armour would stop needing to answer waves at all, and one round would make it a trinket. Graded on the scripted pressure test rather than on a session: nothing has *played* a run that salvages hard enough to afford both, so the effect is confirmed and the price is not. |
| enclave `at_paces` | 8,000 into region 2 | MEASURED | Measured end to end (`examples/journey.rs`): 12 seeds of 12 reach the Refugia in **129-147 minutes at 1x**, crossing 4 forks and berthing at 3 of 3 settlements. Every settlement is reached and traded at, none is missed. About seven minutes of 1× walking past the boundary — one or two of the city's ruins, so the normal case is arriving with something to trade, while the remaining four-fifths of the region is still ahead of you to be provisioned for. A considered departure from `v2-plan.md` §6.6's "enclaves between regions": on the boundary, the enclave puts scrap's only consumer *upstream* of the only region that produces much scrap, so everything salvaged past it is dead weight and the milestone's headline mechanic pays out only for region 1's scarce ruins. Worse, it compounds — region 2 halves bamboo yield, berthing costs bamboo the tower did not walk past, and `threat_pct` is higher — so a tower that engaged with salvage would arrive in the hard region poorer, louder, and with no restock ahead of it. Kept clear of the first fork (15,000) by 7,000, comfortably outside `fork_edge_margin_paces`. |
| enclave `offers` | 4 scrap → 3 poles ×20 · 10 bamboo → 4 poles ×10 · 4 poles → 6 darts ×12 | MEASURED | Measured end to end (`examples/journey.rs`): 12 seeds of 12 reach the Refugia in **129-147 minutes at 1x**, crossing 4 forks and berthing at 3 of 3 settlements. 3 trades and 2 recruits taken across a run, ending with 5 crew from a starting 3. Every rate is deliberately worse than the chain's own — the mill turns bamboo into poles one for one and the thornwright turns a pole into three darts — so the enclave is where a tower that lacks a room buys around the gap once, not a substitute for building the room. Finite stock makes it a windfall rather than an exchange to farm. The scrap rate is the load-bearing one: it is the payoff for the whole of §3.4 and the reason scrap exists in M3 at all, and at three poles for four units a 40-unit ruin fetches 30 poles, which is exactly the recruit price below. Its stock of 20 caps the board at 60 poles for 80 units of scrap — two or three good ruins' worth, so a player who berthed at everything on the way in still has scrap left over that banks against M5's forge. |
| enclave `recruits` / `recruit_cost` | 1 / 30 poles | MEASURED | Measured end to end (`examples/journey.rs`): 12 seeds of 12 reach the Refugia in **129-147 minutes at 1x**, crossing 4 forks and berthing at 3 of 3 settlements. Five recruits offered across three settlements against a `crew_cap` of 8 and a starting crew of 3, so the cap is reachable by a run that spends everything on people and by no other. One pair of hands, priced steeply, and exactly one: `starting_crew` was the first constant in the game graded `PLAYTESTED`, and what it measured was that going from two crew to three moved throughput by ninety percent. A fourth is the largest single change a player can buy and should cost like it. 30 poles is five floors, or a thornwright and a battery and change — and, not by coincidence, exactly what a 40-unit ruin fetches at the scrap rate above. One good berth on the city's edge is one recruit, which is the trade the whole walk into region 2 is arranged around. The new crew member takes the next placeholder name by index rather than by a roll, so recruiting perturbs no RNG stream (`DECISIONS.md` §2). |
